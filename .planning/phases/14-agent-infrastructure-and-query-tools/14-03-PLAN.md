---
phase: 14-agent-infrastructure-and-query-tools
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified:
  - packages/server/src/agent/tools/query-tools.ts
autonomous: true
requirements:
  - QUERY-05
  - QUERY-06
  - QUERY-07
  - QUERY-08
  - QUERY-09
  - SAFE-04

must_haves:
  truths:
    - "Agent can search and filter transactions by payee, category, date range, amount, with pagination"
    - "Agent can list uncategorized transactions"
    - "Agent can show sync status with last sync time and errors"
    - "Agent can list pending transfer suggestions"
    - "Agent can list categorization rules"
    - "Bank-sourced strings (merchant names, memos) are wrapped in XML delimiters in tool output"
  artifacts:
    - path: "packages/server/src/agent/tools/query-tools.ts"
      provides: "5 additional query tools (total 11)"
      exports: ["createQueryTools"]
  key_links:
    - from: "packages/server/src/agent/tools/query-tools.ts"
      to: "packages/server/src/rules/rules-service.ts"
      via: "listRules import"
      pattern: "import.*rules-service"
    - from: "packages/server/src/agent/tools/query-tools.ts"
      to: "packages/server/src/transfers/transfer-service.ts"
      via: "listTransferCandidates import"
      pattern: "import.*transfer-service"
    - from: "packages/server/src/agent/tools/query-tools.ts"
      to: "packages/server/src/categories/category-service.ts"
      via: "listGroupsWithCategories import"
      pattern: "import.*category-service"
---

<objective>
Add the remaining 5 query tools: list_transactions, get_uncategorized_transactions, get_sync_status, get_transfer_suggestions, list_categories, and list_rules. This completes the full 11-tool query suite.

Purpose: Covers all QUERY requirements, enabling the agent to answer any read-only financial question.
Output: All 11 query tools registered and functional.
</objective>

<execution_context>
@/Users/seanspade/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seanspade/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/14-agent-infrastructure-and-query-tools/14-RESEARCH.md
@.planning/phases/14-agent-infrastructure-and-query-tools/14-01-SUMMARY.md
@.planning/phases/14-agent-infrastructure-and-query-tools/14-02-SUMMARY.md
@packages/server/src/agent/tools/query-tools.ts
@packages/server/src/rules/rules-service.ts
@packages/server/src/transfers/transfer-service.ts
@packages/server/src/categories/category-service.ts

<interfaces>
<!-- Service functions to wrap -->

From packages/server/src/rules/rules-service.ts:
```typescript
export interface RuleWithCategory extends Rule {
  categoryName: string;
}
export function listRules(db: Database.Database): RuleWithCategory[];
```

From packages/server/src/transfers/transfer-service.ts:
```typescript
export interface TransferPair {
  id: number;
  transactionA: TransactionSide;
  transactionB: TransactionSide;
  createdAt: string;
}
export function listTransferCandidates(db: Database.Database): TransferPair[];
```

From packages/server/src/categories/category-service.ts:
```typescript
export interface CategoryGroup {
  id: number;
  name: string;
  sort_order: number;
  categories: Category[];
}
export function listGroupsWithCategories(db: Database.Database): CategoryGroup[];
```

Database schema for transactions:
```sql
-- transactions table columns: id, account_id, date, amount, payee, memo, category_id, ...
-- sync_log table columns: id, started_at, completed_at, status, accounts_synced, transactions_added, transactions_updated, error
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add remaining 5 query tools to query-tools.ts</name>
  <files>packages/server/src/agent/tools/query-tools.ts</files>
  <action>
Add 5 more tools to the array returned by `createQueryTools(db)`:

1. **list_transactions** — Input: `{ payee: z.string().optional().describe("Filter by payee name (contains match)"), categoryId: z.number().optional().describe("Filter by category ID"), accountId: z.string().optional().describe("Filter by account ID"), startDate: z.string().optional().describe("Start date YYYY-MM-DD"), endDate: z.string().optional().describe("End date YYYY-MM-DD"), minAmount: z.number().optional().describe("Min amount in cents (use negative for expenses)"), maxAmount: z.number().optional().describe("Max amount in cents"), limit: z.number().optional().default(20).describe("Max results (default 20, max 100)") }`.
   - Description: "Search and filter transactions. Use for finding specific transactions or merchants. Amounts in cents (negative = expense, positive = income). Returns paginated results with total count."
   - Handler: Build dynamic SQL query with WHERE clauses for each optional filter. Payee filter uses `LIKE '%' || ? || '%'` (case-insensitive via COLLATE NOCASE). Clamp limit to max 100. Query with `LIMIT ? + 1` to detect if more exist. Also run a COUNT query for total matching. Wrap bank-sourced strings in XML: `<merchant>${row.payee}</merchant>`, `<memo>${row.memo}</memo>`. Return `{ transactions: [...], total: number, hasMore: boolean }`.

2. **get_uncategorized_transactions** — Input: `{ limit: z.number().optional().default(20).describe("Max results (default 20, max 100)") }`.
   - Description: "List transactions without a category. Use to answer 'what needs categorizing?' Amounts in cents."
   - Handler: Query `SELECT id, account_id, date, amount, payee, memo FROM transactions WHERE category_id IS NULL ORDER BY date DESC LIMIT ?`. Clamp limit to max 100. Wrap payee/memo in XML delimiters. Return JSON array with total count.

3. **list_categories** — No inputs.
   - Description: "List all category groups and their categories with IDs. Use to resolve category names to IDs for other queries."
   - Handler: Call `listGroupsWithCategories(db)`. Return JSON array.

4. **list_rules** — No inputs.
   - Description: "List all categorization rules showing conditions and target category. Use for 'what rules do I have?' questions."
   - Handler: Call `listRules(db)`. Return JSON array. Wrap merchantPattern in XML: `<merchant_pattern>${rule.merchantPattern}</merchant_pattern>`.

5. **get_sync_status** — No inputs.
   - Description: "Get sync status: last sync time, result, and any errors. Use for 'when did I last sync?' or 'are there sync errors?' questions."
   - Handler: Query `SELECT started_at, completed_at, status, accounts_synced, transactions_added, transactions_updated, error FROM sync_log ORDER BY started_at DESC LIMIT 5`. Return JSON array of recent syncs.

6. **get_transfer_suggestions** — No inputs.
   - Description: "List pending (unconfirmed) transfer suggestions between accounts. Use for 'are there any transfers to review?' questions. Amounts in cents."
   - Handler: Call `listTransferCandidates(db)`. Filter to only unconfirmed (the function already does this). Wrap payee fields in XML delimiters. Return JSON array.

For ALL tools:
- Wrap handler in try/catch; on error return `{ isError: true, content: [{ type: "text", text: "Error: " + sanitized message }] }`
- XML-delimit ALL bank-sourced strings (payee, memo, merchantPattern) as `<merchant>...</merchant>`, `<memo>...</memo>`, `<merchant_pattern>...</merchant_pattern>`
  </action>
  <verify>
    <automated>cd /Users/seanspade/Documents/Source/minverva-money && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>query-tools.ts now exports createQueryTools(db) returning 11 tools total, all with Zod-validated inputs, bank-sourced strings XML-delimited, paginated where appropriate</done>
</task>

</tasks>

<verification>
- TypeScript compiles with no errors
- createQueryTools(db) returns exactly 11 tools
- list_transactions supports all 8 filter parameters with pagination
- Bank-sourced strings wrapped in XML delimiters (payee, memo, merchantPattern)
- Limits clamped to max 100
- Error handling returns isError: true
</verification>

<success_criteria>
- 11 total query tools in createQueryTools
- list_transactions: dynamic SQL with optional filters, pagination, XML-wrapped payee/memo
- get_uncategorized_transactions: category_id IS NULL filter, pagination
- list_categories: wraps listGroupsWithCategories
- list_rules: wraps listRules, XML-wrapped merchantPattern
- get_sync_status: queries sync_log for last 5 syncs
- get_transfer_suggestions: wraps listTransferCandidates, XML-wrapped payee
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/14-agent-infrastructure-and-query-tools/14-03-SUMMARY.md`
</output>
