---
phase: 16-action-tools-and-confirmation-flow
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - packages/server/src/agent/mcp-server.ts
  - packages/server/src/agent/agent-service.ts
  - packages/server/src/agent/agent-router.ts
  - packages/server/src/agent/system-prompt.ts
autonomous: true
requirements: [SAFE-02, ACTION-01, ACTION-02, ACTION-03, ACTION-04, ACTION-05, ACTION-06, ACTION-07, ACTION-08]

must_haves:
  truths:
    - "Agent can call action tools via MCP server alongside query tools"
    - "Agent receives tRPC context for sync tool access"
    - "Agent asks for confirmation before executing budget changes"
    - "Agent describes completed write operations and reports affected counts"
    - "Agent confirms rule name before deleting"
  artifacts:
    - path: "packages/server/src/agent/mcp-server.ts"
      provides: "MCP server with both query and action tools"
      exports: ["createMcpServer"]
    - path: "packages/server/src/agent/system-prompt.ts"
      provides: "System prompt with write operation and confirmation rules"
      exports: ["getSystemPrompt"]
  key_links:
    - from: "packages/server/src/agent/agent-router.ts"
      to: "packages/server/src/agent/agent-service.ts"
      via: "passes full tRPC ctx to chat()"
      pattern: "chat\\(ctx"
    - from: "packages/server/src/agent/agent-service.ts"
      to: "packages/server/src/agent/mcp-server.ts"
      via: "passes ctx to createMcpServer()"
      pattern: "createMcpServer\\(db.*ctx"
    - from: "packages/server/src/agent/mcp-server.ts"
      to: "packages/server/src/agent/tools/action-tools.ts"
      via: "spreads createActionTools(db, ctx) into tools array"
      pattern: "createActionTools"
---

<objective>
Wire action tools into the MCP server and update the system prompt with write operation and confirmation rules.

Purpose: Complete the integration so the agent can actually use action tools and follows correct confirmation behavior for budget changes.
Output: Updated mcp-server.ts, agent-service.ts, agent-router.ts, and system-prompt.ts.
</objective>

<execution_context>
@/Users/seanspade/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seanspade/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/16-action-tools-and-confirmation-flow/16-CONTEXT.md
@.planning/phases/16-action-tools-and-confirmation-flow/16-01-SUMMARY.md
@packages/server/src/agent/mcp-server.ts
@packages/server/src/agent/agent-service.ts
@packages/server/src/agent/agent-router.ts
@packages/server/src/agent/system-prompt.ts

<interfaces>
From packages/server/src/agent/tools/action-tools.ts (created in Plan 01):
```typescript
import type { Context } from '../../sync/trpc.js';
export function createActionTools(db: Database.Database, ctx: Context): Tool[];
```

From packages/server/src/sync/trpc.ts:
```typescript
export interface Context { db: Database.Database; rateLimiter: RateLimiter; client: SimpleFINClient; }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire action tools into MCP server and pass context through</name>
  <files>packages/server/src/agent/mcp-server.ts, packages/server/src/agent/agent-service.ts, packages/server/src/agent/agent-router.ts</files>
  <action>
1. Update `mcp-server.ts`:
   - Import `createActionTools` from `./tools/action-tools.js`
   - Import `Context` type from `../sync/trpc.js`
   - Change signature from `createMcpServer(db: Database.Database)` to `createMcpServer(db: Database.Database, ctx: Context)`
   - Add action tools to the tools array: `tools: [...createQueryTools(db), ...createActionTools(db, ctx)]`

2. Update `agent-service.ts`:
   - Import `Context` type from `../sync/trpc.js`
   - Change `chat()` signature from `chat(db, message, sessionId?)` to `chat(db: Database.Database, ctx: Context, message: string, sessionId?: string)`
   - Pass ctx to `createMcpServer(db, ctx)`

3. Update `agent-router.ts`:
   - Change the chat mutation to pass full ctx: `return chat(ctx.db, ctx, input.message, input.sessionId);`
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>MCP server registers action tools alongside query tools, agent-service receives tRPC context, agent-router passes full context</done>
</task>

<task type="auto">
  <name>Task 2: Update system prompt with write operation and confirmation rules</name>
  <files>packages/server/src/agent/system-prompt.ts</files>
  <action>
Add the following rules to the SYSTEM_PROMPT string, after the existing rules section:

```
## Write Operations

8. After executing a write operation, describe what action was taken. Include counts when applicable (e.g., "Categorized the Starbucks transaction as Dining" or "Applied rule — 5 transactions updated").
9. Before deleting a rule, state its name and ask the user to confirm (e.g., "I'll delete the rule 'Starbucks -> Dining'. Should I proceed?").
10. Validate inputs before executing. If a category, rule, or transfer ID doesn't exist, tell the user what went wrong and suggest how to fix it.

## Budget Confirmations

11. Before changing a budget allocation or default, describe the proposed change and include a confirmation block in this exact format:

\`\`\`json
{ "type": "confirmation", "action": "set_budget_allocation", "description": "Set Groceries budget to $500.00 for 2026-03" }
\`\`\`

Only call set_budget_allocation or set_default_allocation AFTER the user confirms. If the user cancels, acknowledge and do not make the change.

12. When the user says amounts in dollars, convert to cents before calling budget tools (e.g., $500 = 50000 cents).
```

Keep the existing 7 rules numbered as-is. Add the new rules numbered 8-12.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>System prompt includes write operation rules (describe actions, confirm deletes) and budget confirmation rules (exact JSON block format, wait for user confirmation before executing)</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` compiles without errors
- `npx vitest run` passes (all tests including query tool tests still pass)
- mcp-server.ts creates MCP server with both query and action tools
- agent-router.ts passes full tRPC context to chat()
- system-prompt.ts includes confirmation block format matching ChatPage regex
</verification>

<success_criteria>
- Action tools registered in MCP server alongside query tools
- tRPC context flows from router -> service -> mcp-server -> action tools
- System prompt instructs agent on write operation behavior
- System prompt includes exact confirmation JSON block format for budget changes
- All existing tests continue to pass
</success_criteria>

<output>
After completion, create `.planning/phases/16-action-tools-and-confirmation-flow/16-02-SUMMARY.md`
</output>
