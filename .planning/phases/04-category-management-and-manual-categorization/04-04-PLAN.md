---
phase: 04-category-management-and-manual-categorization
plan: 04
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - packages/client/src/components/ManualTransactionForm.tsx
  - packages/client/src/pages/TransactionsPage.tsx
autonomous: true
requirements:
  - TXNR-01

must_haves:
  truths:
    - "User can open a form to manually enter a transaction"
    - "Form requires amount, payee, date, and account selection"
    - "User can optionally assign a category to the manual transaction"
    - "After submission, the new transaction appears in the transaction list without page reload"
  artifacts:
    - path: "packages/client/src/components/ManualTransactionForm.tsx"
      provides: "Manual transaction entry form"
      min_lines: 60
    - path: "packages/client/src/pages/TransactionsPage.tsx"
      provides: "Add Transaction button and form integration"
      contains: "ManualTransactionForm"
  key_links:
    - from: "packages/client/src/pages/TransactionsPage.tsx"
      to: "packages/client/src/components/ManualTransactionForm.tsx"
      via: "import and conditional render"
      pattern: "ManualTransactionForm"
    - from: "packages/client/src/components/ManualTransactionForm.tsx"
      to: "trpc.transactions.create"
      via: "useMutation"
      pattern: "transactions\\.create"
---

<objective>
Add a manual transaction entry form to the transactions page so users can enter transactions not captured by bank sync.

Purpose: Fulfills TXNR-01 — users can manually record cash purchases, transfers, or corrections.
Output: ManualTransactionForm component integrated into TransactionsPage.
</objective>

<execution_context>
@/Users/seanspade/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seanspade/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/04-category-management-and-manual-categorization/04-01-SUMMARY.md
@packages/client/src/pages/TransactionsPage.tsx
@packages/client/src/trpc.ts
@packages/client/src/lib/format.ts

<interfaces>
Expected tRPC mutations (from plan 04-01):
```typescript
trpc.transactions.create.mutate({
  accountId: string,
  date: string,       // YYYY-MM-DD
  amount: number,     // integer cents (negative for expenses, positive for income)
  payee: string,
  memo?: string,
  categoryId?: number
})
// Returns: { id: string }
```

Expected accounts.list response:
```typescript
trpc.accounts.list.queryOptions()
// Returns: Array<{ id: string, name: string, institution: string, type: string, balance: number, lastSynced: string | null }>
```

CategoryPicker component (from plan 04-03):
```typescript
<CategoryPicker value={categoryId} onChange={(id) => setCategoryId(id)} />
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ManualTransactionForm and integrate into TransactionsPage</name>
  <files>packages/client/src/components/ManualTransactionForm.tsx, packages/client/src/pages/TransactionsPage.tsx</files>
  <action>
    **Create ManualTransactionForm.tsx:**

    Props:
    - `onClose: () => void` — called when form is submitted or cancelled

    Implementation:
    - Render as an inline form at the top of the transactions list (not a modal — keeps it close to the data).
    - Fields:
      - **Date:** `<input type="date">`, defaults to today (YYYY-MM-DD format)
      - **Payee:** `<input type="text">`, required, placeholder "Payee name"
      - **Amount:** `<input type="text">`, accepts decimal dollar value (e.g., "-45.99" or "100.00"). Convert to integer cents on submission. Negative for expenses, positive for income.
      - **Account:** `<select>` populated from `useQuery(trpc.accounts.list.queryOptions())`. Required.
      - **Category:** `<CategoryPicker>` component (optional, defaults to null/uncategorized)
      - **Memo:** `<input type="text">`, optional, placeholder "Memo (optional)"
    - Buttons:
      - "Save" — validates inputs, calls `trpc.transactions.create.mutate`, invalidates `transactions.list` query, calls `onClose()`
      - "Cancel" — calls `onClose()` without saving
    - Validation:
      - Amount must be non-zero (show error message "Amount is required")
      - Payee must be non-empty (show error message "Payee is required")
      - Account must be selected (show error message "Account is required")
      - Date must be valid (HTML5 date input handles this)
    - Show validation errors inline below each field in red text
    - Convert dollar input to cents: `Math.round(parseFloat(value) * 100)`. Reject NaN.
    - After successful save, reset the form and close

    **Update TransactionsPage.tsx:**
    - Add an "Add Transaction" button next to the page heading
    - Toggle state `showAddForm` (default false)
    - When true, render `<ManualTransactionForm onClose={() => setShowAddForm(false)} />` above the filters
    - When form closes, set showAddForm to false

    Style the form with Tailwind: border, rounded, padding, gray-100 background to distinguish from the table. Fields in a row on desktop (flex-wrap), stacked on mobile.
  </action>
  <verify>
    - "Add Transaction" button appears on transactions page
    - Clicking it reveals the inline form
    - Form validates required fields and shows errors
    - Submitting a valid form creates the transaction (visible in list after cache invalidation)
    - Cancel closes the form without side effects
  </verify>
  <done>
    Manual transaction form appears inline on the transactions page. Required fields are validated. Successful submission creates a transaction via tRPC and it appears in the list. Cancel dismisses the form.
  </done>
</task>

</tasks>

<verification>
- "Add Transaction" button visible on transactions page
- Form validates: non-zero amount, non-empty payee, selected account
- Dollar input correctly converts to integer cents
- New transaction appears in list after submission
- Form resets and closes after successful save
</verification>

<success_criteria>
- ManualTransactionForm renders with all required fields
- Validation prevents submission of incomplete data
- Dollar-to-cents conversion is accurate (Math.round(parseFloat * 100))
- Created transaction has UUID id and null dedup_hash (server-side)
- Transaction list updates without page reload after creation
</success_criteria>

<output>
After completion, create `.planning/phases/04-category-management-and-manual-categorization/04-04-SUMMARY.md`
</output>
