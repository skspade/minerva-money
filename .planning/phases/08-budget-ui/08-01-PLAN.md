---
phase: 08-budget-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/client/src/pages/BudgetPage.tsx
  - packages/client/src/app.tsx
  - packages/client/src/components/Layout.tsx
autonomous: true
requirements: [BUDG-02]

must_haves:
  truths:
    - "Budget page shows a grid of all categories with allocated, spent, and available columns for the selected month"
    - "Categories are grouped by category group in collapsible accordion sections"
    - "Group header rows show group subtotals for allocated, spent, and available"
    - "Available column turns red when a category is overspent (available < 0)"
    - "Positive available values display in green"
    - "User can navigate between months using previous/next arrow buttons"
    - "Budget link appears in the navigation bar"
  artifacts:
    - path: "packages/client/src/pages/BudgetPage.tsx"
      provides: "Budget grid with category groups, month navigation, overspent highlighting"
    - path: "packages/client/src/app.tsx"
      provides: "Budget route at /budget"
    - path: "packages/client/src/components/Layout.tsx"
      provides: "Budget navigation link"
  key_links:
    - from: "packages/client/src/pages/BudgetPage.tsx"
      to: "budget.summary"
      via: "useQuery(trpc.budget.summary.queryOptions({ period }))"
      pattern: "trpc\\.budget\\.summary"
    - from: "packages/client/src/app.tsx"
      to: "packages/client/src/pages/BudgetPage.tsx"
      via: "Route path='budget' element={<BudgetPage />}"
      pattern: "BudgetPage"
---

<objective>
Build the budget grid page with category group accordion, allocated/spent/available columns, overspent highlighting, month navigation, and wire it into the app router and navigation bar.

Purpose: Users can view their full envelope budget for any month, see how much is allocated, spent, and available per category, and quickly identify overspent categories.
Output: BudgetPage.tsx with routing and nav integration.
</objective>

<execution_context>
@/Users/seanspade/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seanspade/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@packages/client/src/pages/CategoriesPage.tsx
@packages/client/src/components/Layout.tsx
@packages/client/src/app.tsx
@packages/client/src/trpc.ts
@packages/client/src/lib/format.ts

<interfaces>
<!-- Key types and contracts the executor needs. -->

From packages/server/src/budget/budget-service.ts:
```typescript
export interface BudgetCategorySummary {
  categoryId: number;
  categoryName: string;
  groupName: string;
  allocated: number;   // integer cents
  spent: number;       // integer cents
  available: number;   // allocated + rollover - spent
  rollover: number;    // integer cents
}
```

From packages/server/src/sync/trpc-router.ts (budget.summary):
```typescript
// budget.summary input: { period: "YYYY-MM" }
// budget.summary output: { categories: BudgetCategorySummary[], availableToBudget: number }
```

From packages/client/src/lib/format.ts:
```typescript
export function formatCurrency(cents: number): string;
```

From packages/client/src/trpc.ts:
```typescript
export const { TRPCProvider, useTRPC } = createTRPCContext<AppRouter>();
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create BudgetPage with budget grid and month navigation</name>
  <files>packages/client/src/pages/BudgetPage.tsx</files>
  <action>
Create `BudgetPage.tsx` in `packages/client/src/pages/`.

**Month navigation state:**
- Store selected period as `YYYY-MM` string in component state, defaulting to the current month (`new Date()` formatted)
- Display current month as "March 2026" format using `new Date(period + '-01').toLocaleDateString('en-US', { month: 'long', year: 'numeric' })`
- Left/right arrow buttons to decrement/increment month
- Helper functions to compute previous/next period strings

**Data fetching:**
- Use `useQuery(trpc.budget.summary.queryOptions({ period }))` to fetch budget data
- Standard loading state: `<p className="text-gray-500">Loading budget...</p>`
- Standard error state: `<p className="text-red-600">Error: {message}</p>`

**Budget grid layout:**
- Group categories by `groupName` from the summary response
- Each group renders as a collapsible section with a header row and category rows
- Group header row: group name with collapse toggle (use `collapsed` state per group, same pattern as CategoriesPage), plus group subtotals for allocated, spent, and available
- Category rows inside each group: category name, allocated amount, spent amount, available amount
- All money values displayed using `formatCurrency()` from `packages/client/src/lib/format.ts`
- Table-like layout using Tailwind with consistent column widths. Use a grid or table structure where columns align across all groups.

**Overspent highlighting (per CONTEXT.md locked decisions):**
- Available column: red text (`text-red-600`) and subtle red background (`bg-red-50`) when available < 0
- Available column: green text (`text-green-600`) when available > 0
- Available column: default gray text when available === 0

**Column headers:** Show "Category", "Allocated", "Spent", "Available" as a sticky header row above all groups.
  </action>
  <verify>
    <automated>cd packages/client && npx tsc --noEmit</automated>
  </verify>
  <done>BudgetPage renders a grouped budget grid with month navigation, collapsible groups with subtotals, and color-coded available amounts.</done>
</task>

<task type="auto">
  <name>Task 2: Wire BudgetPage into router and navigation</name>
  <files>packages/client/src/app.tsx, packages/client/src/components/Layout.tsx</files>
  <action>
**In app.tsx:**
- Import `BudgetPage` from `./pages/BudgetPage`
- Add `<Route path="budget" element={<BudgetPage />} />` inside the Layout route, after the transfers route

**In Layout.tsx:**
- Add a `<NavLink to="/budget">Budget</NavLink>` in the navigation bar, after the Transfers link
- Use the same `className` pattern as existing NavLinks (isActive conditional for bg-gray-700)
  </action>
  <verify>
    <automated>cd packages/client && npx tsc --noEmit</automated>
  </verify>
  <done>Navigating to /budget in the browser shows the BudgetPage. Budget link appears in the navbar and highlights when active.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes in client package (no type errors)
- Budget page loads at /budget and displays category grid grouped by category group
- Month navigation changes the displayed period and re-fetches data
- Groups are collapsible with toggle
- Group subtotals show sum of allocated/spent/available for categories in that group
- Overspent categories show red available amount with red background
- Positive available amounts show in green
- Budget link visible in navbar
</verification>

<success_criteria>
- BudgetPage.tsx exists and renders budget grid with grouped categories
- Month navigation works with previous/next arrows
- Overspent highlighting applies red styling to negative available values
- Route registered at /budget in app.tsx
- Budget NavLink added to Layout.tsx
</success_criteria>

<output>
After completion, create `.planning/phases/08-budget-ui/08-01-SUMMARY.md`
</output>
