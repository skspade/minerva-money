---
phase: 49-trpc-response-extension
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - packages/server/src/sync/trpc-router.ts
  - packages/server/src/sync/sync-status.test.ts
autonomous: true
requirements: [API-01, API-02]

must_haves:
  truths:
    - sync.status response includes a `warnings` array with objects containing accountId, accountName, errorCode, message, lastSeen
    - When no warnings exist, response includes `warnings: []`
    - Existing lastSync, errorCount, and accounts fields are unchanged
  artifacts:
    - packages/server/src/sync/sync-status.test.ts
  key_links:
    - sync_warnings table queried in sync.status handler
    - snake_case DB columns mapped to camelCase in response
---

<objective>
Extend the sync.status tRPC query to include a warnings array from the sync_warnings table.

Purpose: Downstream phases (50-52) need structured per-account warnings from the existing sync.status endpoint. This is the data contract that Dashboard, Navbar, and Agent phases will consume.
Output: Updated sync.status handler returning warnings alongside existing fields, with tests.
</objective>

<execution_context>
@/Users/seanspade/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seanspade/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-trpc-response-extension/49-CONTEXT.md
@.planning/phases/49-trpc-response-extension/49-RESEARCH.md
@packages/server/src/sync/trpc-router.ts
@packages/server/migrations/007-sync-warnings.sql

<interfaces>
<!-- sync.status handler: trpc-router.ts lines 80-119 -->
<!-- Current return shape: { lastSync: {...} | null, errorCount: number, accounts: [...] } -->
<!-- Pattern: ctx.db.prepare(SQL).get/all() with inline `as` casts, snake_case -> camelCase mapping -->

<!-- sync_warnings table columns: -->
<!-- id, sync_log_id, account_id, account_name, error_code, message, first_seen, last_seen, occurrence_count -->
<!-- UNIQUE(account_id) — at most one active warning per account -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write tests for sync.status warnings array</name>
  <files>packages/server/src/sync/sync-status.test.ts</files>
  <behavior>
    - Test 1: sync.status returns warnings as empty array when no warnings exist in sync_warnings table
    - Test 2: sync.status returns warnings array with correct shape (accountId, accountName, errorCode, message, lastSeen) when sync_warnings has entries
    - Test 3: warnings are ordered by lastSeen descending (most recent first)
    - Test 4: existing fields (lastSync, errorCount, accounts) are still present and correct when warnings exist
  </behavior>
  <action>
    Create `packages/server/src/sync/sync-status.test.ts`. Use the existing test patterns in the codebase:
    - Import `Database` from `better-sqlite3` and create an in-memory database
    - Run migrations to set up schema (read migration files from `packages/server/migrations/` and execute them in order)
    - Create the tRPC caller using the test context pattern
    - Seed test data: insert sync_log entries, accounts, and sync_warnings rows
    - Call `caller.sync.status()` and assert the response shape

    For the empty warnings test: just ensure the base schema is set up with no sync_warnings rows.
    For the populated test: insert 2-3 sync_warnings rows with different last_seen values.
    For ordering: verify the first element has the most recent last_seen.
    For backward compatibility: verify lastSync, errorCount, accounts still work as before.

    These tests should RED initially (warnings field doesn't exist yet).
  </action>
  <verify>
    <automated>npx vitest run packages/server/src/sync/sync-status.test.ts</automated>
  </verify>
  <done>Tests exist and fail with clear errors about missing `warnings` property on sync.status response</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add warnings query to sync.status handler</name>
  <files>packages/server/src/sync/trpc-router.ts</files>
  <action>
    In `packages/server/src/sync/trpc-router.ts`, modify the `sync.status` query handler (line 80-119):

    1. After the existing `accounts` query (line 97-105), add a warnings query:
    ```typescript
    const warnings = ctx.db.prepare(
      'SELECT account_id, account_name, error_code, message, last_seen FROM sync_warnings ORDER BY last_seen DESC'
    ).all() as {
      account_id: string;
      account_name: string;
      error_code: string;
      message: string;
      last_seen: string;
    }[];
    ```

    2. Add `warnings` to the return object (after `accounts`):
    ```typescript
    warnings: warnings.map(w => ({
      accountId: w.account_id,
      accountName: w.account_name,
      errorCode: w.error_code,
      message: w.message,
      lastSeen: w.last_seen,
    })),
    ```

    This follows the exact same pattern as the existing queries: inline `db.prepare().all()`, TypeScript `as` cast, snake_case-to-camelCase mapping in the return object.

    Do NOT:
    - Extract to a service layer (matches existing inline pattern)
    - Add Zod output schema (not used anywhere in this codebase)
    - Include first_seen or occurrence_count (not required by API-01)
    - Change any existing fields
  </action>
  <verify>
    <automated>npx vitest run packages/server/src/sync/sync-status.test.ts</automated>
  </verify>
  <done>All tests pass. sync.status returns { lastSync, errorCount, accounts, warnings } where warnings is an array of { accountId, accountName, errorCode, message, lastSeen } objects ordered by lastSeen DESC, or empty array when no warnings exist.</done>
</task>

</tasks>

<verification>
- `npx vitest run packages/server/src/sync/sync-status.test.ts` — all tests pass
- `npm run build` — TypeScript compilation succeeds (type inference propagates to client)
- Verify the sync.status response type now includes `warnings` array via tRPC type inference
</verification>

<success_criteria>
- sync.status tRPC response includes warnings array with accountId, accountName, errorCode, message, lastSeen per entry (API-01)
- Warnings queried from sync_warnings table alongside existing sync status fields (API-02)
- Empty array returned when no warnings exist
- Existing lastSync, errorCount, accounts fields unchanged
- All tests pass
- Build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/49-trpc-response-extension/49-01-SUMMARY.md`
</output>
