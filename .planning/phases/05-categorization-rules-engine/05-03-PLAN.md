---
phase: 05-categorization-rules-engine
plan: 03
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  - packages/client/src/pages/RulesPage.tsx
  - packages/client/src/components/RuleForm.tsx
  - packages/client/src/components/RetroactivePreview.tsx
  - packages/client/src/app.tsx
  - packages/client/src/components/Layout.tsx
autonomous: true
requirements:
  - CATG-02
  - CATG-03

must_haves:
  truths:
    - "User can see a list of all rules with name, conditions, target category, and specificity score"
    - "User can create a new rule with merchant, amount range, and memo conditions"
    - "After creating a rule, a preview shows matching transactions before applying"
    - "User can confirm retroactive application and see updated count"
    - "User can edit and delete existing rules"
    - "Rules page is accessible from the navigation bar"
  artifacts:
    - path: "packages/client/src/pages/RulesPage.tsx"
      provides: "Rules list page with create/edit/delete actions"
    - path: "packages/client/src/components/RuleForm.tsx"
      provides: "Create/edit form with merchant, amount, memo fields and category picker"
    - path: "packages/client/src/components/RetroactivePreview.tsx"
      provides: "Preview modal showing transactions that would be recategorized"
  key_links:
    - from: "packages/client/src/pages/RulesPage.tsx"
      to: "trpc.rules.list"
      via: "useQuery"
      pattern: "trpc\\.rules\\.list"
    - from: "packages/client/src/components/RuleForm.tsx"
      to: "trpc.rules.create"
      via: "useMutation"
      pattern: "trpc\\.rules\\.create"
    - from: "packages/client/src/components/RetroactivePreview.tsx"
      to: "trpc.rules.preview"
      via: "useQuery"
      pattern: "trpc\\.rules\\.preview"
---

<objective>
Build the rules management UI: rules list page, create/edit form with all condition types, and retroactive preview modal with apply confirmation.

Purpose: This is the primary user interface for creating and managing categorization rules with the preview-before-apply workflow.
Output: Complete rules management page with create, edit, delete, and retroactive preview/apply.
</objective>

<execution_context>
@/Users/seanspade/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seanspade/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-categorization-rules-engine/05-02-SUMMARY.md
@packages/client/src/app.tsx
@packages/client/src/components/Layout.tsx
@packages/client/src/components/CategoryPicker.tsx
@packages/client/src/lib/format.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rules list page and navigation</name>
  <files>packages/client/src/pages/RulesPage.tsx, packages/client/src/app.tsx, packages/client/src/components/Layout.tsx</files>
  <action>
    Create RulesPage.tsx with:
    - useQuery for trpc.rules.list to fetch all rules
    - Table layout showing: name, conditions summary (e.g., "Merchant contains 'Amazon', Amount $50-$100"), target category name, specificity score
    - "Create Rule" button that opens the rule form (use state toggle, inline below header)
    - Each rule row has Edit and Delete buttons
    - Delete uses useMutation for trpc.rules.delete with confirmation (window.confirm)
    - After delete, invalidate rules query
    - Conditions summary: format merchant as "Merchant = 'X'" or "Merchant contains 'X'" based on match_type, format amount range with formatCurrency, format memo as "Memo contains 'X'"
    - Empty state: "No rules created yet. Create your first rule to auto-categorize transactions."

    Add /rules route to app.tsx Routes.
    Add "Rules" NavLink to Layout.tsx navigation bar (after Categories link).
  </action>
  <verify>
    <automated>npx vitest run</automated>
  </verify>
  <done>Rules page shows list of rules, Create/Edit/Delete actions work, route and nav link added</done>
</task>

<task type="auto">
  <name>Task 2: Rule create/edit form and retroactive preview</name>
  <files>packages/client/src/components/RuleForm.tsx, packages/client/src/components/RetroactivePreview.tsx, packages/client/src/pages/RulesPage.tsx</files>
  <action>
    Create RuleForm.tsx:
    - Props: onSaved callback, optional initialRule for edit mode
    - Fields: rule name (text, required), merchant pattern (text, optional), match type (toggle/select: exact or contains, default contains), amount min (number input in dollars, convert to cents on submit), amount max (number input in dollars, convert to cents), memo pattern (text, optional), target category (CategoryPicker component)
    - Validation: at least one condition (merchant, amount range, or memo) must be provided. Show error message if all conditions are empty.
    - On submit: call trpc.rules.create (or trpc.rules.update for edit mode) via useMutation
    - After successful create: open RetroactivePreview modal passing the new rule ID
    - After successful update: invalidate rules query and close form
    - Cancel button to close form

    Create RetroactivePreview.tsx:
    - Props: ruleId (number), onClose callback
    - useQuery for trpc.rules.preview with ruleId
    - Display as a modal overlay
    - Show table of matching transactions: date, payee, amount, current category (or "Uncategorized"), proposed category
    - If no matches: "No existing transactions match this rule."
    - "Apply to {N} transactions" button calls trpc.rules.apply mutation
    - After apply: show success message with count, invalidate transactions and rules queries, close modal
    - "Skip" button to close without applying

    Wire into RulesPage.tsx:
    - Create mode: show RuleForm when "Create Rule" is clicked
    - Edit mode: show RuleForm with initialRule when Edit is clicked on a rule row
    - RetroactivePreview opens after create via RuleForm callback
  </action>
  <verify>
    <automated>npx vitest run</automated>
  </verify>
  <done>User can create rules with all condition types, see retroactive preview after creation, apply or skip, edit existing rules</done>
</task>

</tasks>

<verification>
- Rules page loads and displays rules from the API
- Create form validates at least one condition
- After creating a rule, retroactive preview shows matching transactions
- Apply button updates transactions and shows count
- Edit and delete work correctly
- Navigation link appears in header
- `npx vitest run` passes all tests
</verification>

<success_criteria>
- Rules page accessible at /rules with navigation link
- Rule list shows name, conditions, category, specificity score
- Create form has merchant (exact/contains), amount range, memo, category picker
- Retroactive preview modal appears after rule creation
- Apply retroactively updates transactions and invalidates caches
- Edit reuses create form with pre-filled values
- Delete with confirmation
</success_criteria>

<output>
After completion, create `.planning/phases/05-categorization-rules-engine/05-03-SUMMARY.md`
</output>
