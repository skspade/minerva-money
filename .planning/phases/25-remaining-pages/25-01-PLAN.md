---
phase: 25-remaining-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/client/src/pages/ReportsPage.tsx
  - packages/client/src/pages/ChatPage.tsx
  - packages/client/src/pages/CategoriesPage.tsx
autonomous: true
requirements:
  - PAGE-03
  - PAGE-04
  - PAGE-05

must_haves:
  truths:
    - "Reports page date filter row does not overflow at 375px — inputs stack or wrap"
    - "Reports page charts are readable at 375px with no label clipping"
    - "Chat input bar sits above the bottom tab bar on mobile with safe area clearance"
    - "Chat page fills the correct height on mobile using dvh units"
    - "Categories page drag handles have 44px minimum tap targets on mobile"
  artifacts:
    - path: "packages/client/src/pages/ReportsPage.tsx"
      provides: "Mobile-friendly date filter row and chart sizing"
    - path: "packages/client/src/pages/ChatPage.tsx"
      provides: "Mobile height fix and safe area inset on input bar"
    - path: "packages/client/src/pages/CategoriesPage.tsx"
      provides: "44px drag handle tap targets on mobile"
  key_links:
    - from: "ReportsPage.tsx"
      to: "Tailwind max-md: breakpoint"
      via: "CSS class overrides"
      pattern: "max-md:"
    - from: "ChatPage.tsx"
      to: "BottomTabBar safe area"
      via: "env(safe-area-inset-bottom) on input bar"
      pattern: "safe-area-inset-bottom"
---

<objective>
Fix mobile layout issues on ReportsPage (date filter overflow, chart sizing), ChatPage (height calculation, safe area inset on input bar), and CategoriesPage (drag handle tap targets).

Purpose: These three pages have specific mobile issues identified in research — they need targeted CSS fixes using the established `max-md:` Tailwind pattern.
Output: Three page files with mobile-responsive overrides applied.
</objective>

<execution_context>
@/Users/seanspade/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seanspade/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-remaining-pages/25-RESEARCH.md
@packages/client/src/pages/ReportsPage.tsx
@packages/client/src/pages/ChatPage.tsx
@packages/client/src/pages/CategoriesPage.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix ReportsPage and ChatPage mobile layouts</name>
  <files>packages/client/src/pages/ReportsPage.tsx, packages/client/src/pages/ChatPage.tsx</files>
  <action>
**ReportsPage.tsx:**
1. Find the header/filter row (likely a `flex items-center justify-between` div containing the "Reports" heading and date inputs). Add `max-md:flex-col max-md:items-start max-md:gap-3` to stack heading above filters on mobile.
2. On the date inputs container, add `max-md:flex-wrap max-md:w-full` so the From/To inputs wrap if needed. Add `max-md:flex-1` on each date input so they fill available width.
3. Check the PieChart `outerRadius` prop — if it's 120 or higher, reduce it on mobile. Since we cannot use JS breakpoint hooks (project decision: CSS-only responsive), the simplest fix is to reduce `outerRadius` to a value that works at both sizes (e.g., 100), OR remove the inline `label` prop from PieChart and rely on the existing `<Legend />` component for category identification. Prefer removing inline labels if a Legend already exists.
4. Ensure all date inputs have `text-base` class for iOS zoom prevention (TOUCH-02 pattern from Phase 22).

**ChatPage.tsx:**
1. Find the outer container with `h-[calc(100vh-56px)]` height. Change to `h-[calc(100dvh-56px)] md:h-[calc(100vh-56px)]` — use `dvh` on mobile (handles iOS Safari URL bar) and keep `vh` on desktop.
2. Find the input bar div (likely `border-t bg-white px-4 py-3` or similar). Replace fixed bottom padding with `pb-[max(0.75rem,env(safe-area-inset-bottom))]` to clear the iPhone home indicator on notched devices. Keep `pt-3` for top padding.
3. The ChatPage breaks out of main padding with negative margins (`-mx-4 -mt-6`). The `pb-20` on `<main>` does NOT apply inside ChatPage since it fills full height itself. The input bar must handle its own safe area — confirm this is the case after changes.

Do NOT add JS breakpoint hooks — use only CSS/Tailwind classes for all responsive behavior.
  </action>
  <verify>
    <automated>cd /Users/seanspade/Documents/Source/minerva-money && npx tsc --noEmit --project packages/client/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <done>ReportsPage date filters stack vertically on mobile with no overflow. ChatPage uses dvh for mobile height and input bar has safe area inset padding.</done>
</task>

<task type="auto">
  <name>Task 2: Fix CategoriesPage drag handle tap targets</name>
  <files>packages/client/src/pages/CategoriesPage.tsx</files>
  <action>
1. Find the drag handle buttons in both `SortableCategory` and `SortableGroup` components (or equivalent). These render the `⠿` glyph as a drag handle with dnd-kit `{...attributes} {...listeners}` props.
2. Add mobile tap target classes to each drag handle button: `max-md:min-h-[44px] max-md:min-w-[44px] max-md:flex max-md:items-center max-md:justify-center`
3. Keep the existing `cursor-grab text-gray-400 hover:text-gray-600` classes and all dnd-kit props unchanged.
4. Do NOT change `activationConstraint: { distance: 5 }` — the 44px tap target and 5px drag threshold coexist safely (per research Pitfall 3).
5. Verify there are no other interactive elements on the page that fall below 44px tap targets on mobile.
  </action>
  <verify>
    <automated>cd /Users/seanspade/Documents/Source/minerva-money && npx tsc --noEmit --project packages/client/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <done>All drag handle buttons on CategoriesPage have 44px minimum tap targets on mobile via max-md: Tailwind classes.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with no errors in the client package
2. ReportsPage: date filter row uses `max-md:flex-col` or similar stacking
3. ChatPage: outer div uses `dvh` unit; input bar has `safe-area-inset-bottom`
4. CategoriesPage: drag handle buttons include `max-md:min-h-[44px] max-md:min-w-[44px]`
</verification>

<success_criteria>
- ReportsPage renders without horizontal overflow at 375px (date inputs stack/wrap)
- ChatPage input bar is positioned above the bottom tab bar on mobile with safe area inset
- CategoriesPage drag handles meet 44px minimum tap target on mobile
- No TypeScript compilation errors
- No changes affect desktop layout (all overrides use max-md: prefix)
</success_criteria>

<output>
After completion, create `.planning/phases/25-remaining-pages/25-01-SUMMARY.md`
</output>
