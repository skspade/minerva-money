---
phase: 18-production-build-and-directory-layout
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - deploy/com.minerva.server.plist
  - deploy/com.minerva.backup.plist
autonomous: true
requirements:
  - DIR-01
  - DIR-02
must_haves:
  truths:
    - "Server plist uses the correct nvm node binary path"
    - "Backup plist runs compiled JavaScript instead of TypeScript source via tsx"
    - "Backup plist loads environment variables via --env-file=.env"
    - "Both plists live in deploy/ directory with no deployment artifacts elsewhere"
  artifacts:
    - path: "deploy/com.minerva.server.plist"
      provides: "Server launchd service definition with correct node path"
    - path: "deploy/com.minerva.backup.plist"
      provides: "Backup launchd service definition running compiled JS"
  key_links:
    - from: "deploy/com.minerva.server.plist"
      to: "packages/server/dist/index.js"
      via: "ProgramArguments with node --env-file"
      pattern: "packages/server/dist/index.js"
    - from: "deploy/com.minerva.backup.plist"
      to: "packages/server/dist/backup/run-backup.js"
      via: "ProgramArguments with node --env-file"
      pattern: "packages/server/dist/backup/run-backup.js"
---

<objective>
Fix launchd plists for production use and verify deployment directory layout

Purpose: Both plists currently have issues that prevent them from working in production: wrong node binary path (both) and tsx dependency in backup plist. Fix these so services can actually start under launchd on the nvm-managed machine. Also verify the deploy/ directory is the single location for all deployment config.
Output: Corrected plist files ready for launchd installation.
</objective>

<execution_context>
@/Users/seanspade/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seanspade/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@deploy/com.minerva.server.plist
@deploy/com.minerva.backup.plist
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix node binary path and backup plist entry point</name>
  <files>deploy/com.minerva.server.plist, deploy/com.minerva.backup.plist</files>
  <action>
**Server plist** (`deploy/com.minerva.server.plist`):
Change the node binary path from `/usr/local/bin/node` to the actual nvm path. Run `which node` to get the current path (expected: `/Users/seanspade/.nvm/versions/node/v20.16.0/bin/node`). Update the first `<string>` in `ProgramArguments` array.

No other changes to the server plist — the rest of the configuration (WorkingDirectory, KeepAlive, RunAtLoad, ThrottleInterval, log paths, env vars, --env-file) is already correct.

**Backup plist** (`deploy/com.minerva.backup.plist`):
Three changes needed:

1. Change node binary path from `/usr/local/bin/node` to the nvm path (same as server plist).

2. Replace the ProgramArguments array. Current:
```xml
<string>/usr/local/bin/node</string>
<string>--import</string>
<string>tsx</string>
<string>/Users/seanspade/Documents/Source/minverva-money/packages/server/src/backup/run-backup.ts</string>
```
Replace with:
```xml
<string>{nvm_node_path}</string>
<string>--env-file=.env</string>
<string>packages/server/dist/backup/run-backup.js</string>
```
This switches from tsx TypeScript execution to compiled JavaScript, adds --env-file for environment loading, and uses a relative path (WorkingDirectory is already set to repo root).

3. Add EnvironmentVariables dict with NODE_ENV=production (matching server plist pattern):
```xml
<key>EnvironmentVariables</key>
<dict>
    <key>NODE_ENV</key>
    <string>production</string>
</dict>
```
Add this after the WorkingDirectory entry.
  </action>
  <verify>
    <automated>grep -q ".nvm/versions/node" deploy/com.minerva.server.plist && grep -q ".nvm/versions/node" deploy/com.minerva.backup.plist && grep -q "run-backup.js" deploy/com.minerva.backup.plist && grep -q "env-file" deploy/com.minerva.backup.plist && ! grep -q "tsx" deploy/com.minerva.backup.plist && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Both plists use the correct nvm node binary path. Backup plist runs compiled JS via `packages/server/dist/backup/run-backup.js` with `--env-file=.env` and `NODE_ENV=production`. No tsx dependency in any plist.</done>
</task>

<task type="auto">
  <name>Task 2: Verify deployment directory layout</name>
  <files></files>
  <action>
Verify that all deployment configuration lives exclusively in the `deploy/` directory:

1. List contents of `deploy/` — should contain exactly: com.minerva.server.plist, com.minerva.backup.plist, setup.sh, deploy.sh
2. Search the entire repo for any plist files outside `deploy/`: `find . -name "*.plist" -not -path "./deploy/*" -not -path "./node_modules/*" -not -path "./.git/*"`
3. Search for any shell scripts that look deployment-related outside `deploy/`: check for `launchctl`, `launchd`, or service management commands in shell scripts outside deploy/
4. Verify no stray deployment artifacts in repo root or other directories

This is a verification-only task. If any deployment artifacts are found outside `deploy/`, report them but do not move them (that would be out of scope for this plan's files_modified).
  </action>
  <verify>
    <automated>test $(find . -name "*.plist" -not -path "./deploy/*" -not -path "./node_modules/*" -not -path "./.git/*" 2>/dev/null | wc -l) -eq 0 && echo "PASS: all plists in deploy/" || echo "FAIL: plists found outside deploy/"</automated>
  </verify>
  <done>All deployment config (plists, setup.sh, deploy.sh) is co-located in the `deploy/` directory. No deployment artifacts exist elsewhere in the repo.</done>
</task>

</tasks>

<verification>
- Both plists reference the nvm node binary (not /usr/local/bin/node)
- Backup plist runs compiled JS (not TypeScript via tsx)
- Backup plist includes --env-file=.env
- Backup plist includes NODE_ENV=production environment variable
- All deployment files are in deploy/ directory
- No deployment artifacts elsewhere in the repo
</verification>

<success_criteria>
Both plists are production-ready with correct node binary path, backup runs compiled JavaScript with proper environment loading, and all deployment config is co-located in deploy/.
</success_criteria>

<output>
After completion, create `.planning/phases/18-production-build-and-directory-layout/18-02-SUMMARY.md`
</output>
