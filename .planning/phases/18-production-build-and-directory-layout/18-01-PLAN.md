---
phase: 18-production-build-and-directory-layout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/server/package.json
  - packages/shared/package.json
autonomous: true
requirements:
  - BUILD-01
  - BUILD-02
  - BUILD-03
  - BUILD-04
must_haves:
  truths:
    - "npm run build produces clean output with no stale test files in packages/server/dist/"
    - "node packages/server/dist/index.js starts without errors and serves the SPA"
    - "All client-side routes return index.html from the compiled server"
    - "Environment variables load via --env-file with no dotenv dependency"
  artifacts:
    - path: "packages/server/dist/index.js"
      provides: "Compiled server entry point"
    - path: "packages/client/dist/index.html"
      provides: "Bundled client SPA"
    - path: "packages/shared/dist/types.js"
      provides: "Compiled shared types"
  key_links:
    - from: "packages/server/dist/index.js"
      to: "packages/client/dist/"
      via: "express.static and SPA catch-all"
      pattern: "express\\.static.*client/dist"
    - from: "root package.json start:prod"
      to: "packages/server/dist/index.js"
      via: "node --env-file=.env"
      pattern: "node --env-file=.env packages/server/dist/index.js"
---

<objective>
Fix production build pipeline to produce clean compiled output

Purpose: Ensure `npm run build` produces correct, clean output with no stale artifacts, and that the compiled server correctly serves the React SPA with environment variables loaded via Node 20 native `--env-file`.
Output: Clean build pipeline with prebuild clean step, verified production server startup.
</objective>

<execution_context>
@/Users/seanspade/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seanspade/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@packages/server/package.json
@packages/shared/package.json
@packages/server/tsconfig.json
@packages/shared/tsconfig.json
@packages/server/src/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add prebuild clean step to server and shared packages</name>
  <files>packages/server/package.json, packages/shared/package.json</files>
  <action>
Add a `prebuild` script to both `packages/server/package.json` and `packages/shared/package.json` that removes the `dist/` directory before compilation:

In packages/server/package.json, add to scripts:
```json
"prebuild": "rm -rf dist"
```

In packages/shared/package.json, add to scripts:
```json
"prebuild": "rm -rf dist"
```

npm automatically runs `prebuild` before `build`, so no other changes needed. This ensures stale test files (32 currently in server dist from before tsconfig exclude was added) are cleaned on every build.

Do NOT add prebuild to the client package — Vite already handles clean output via its own build process.
  </action>
  <verify>
    <automated>rm -rf packages/server/dist packages/shared/dist && npm run build && test ! -f packages/server/dist/index.test.js && echo "PASS: no stale test files" || echo "FAIL: stale test files found"</automated>
  </verify>
  <done>Running `npm run build` produces packages/server/dist/ with no .test.js or .test.d.ts files. packages/shared/dist/ contains only types.js and types.d.ts.</done>
</task>

<task type="auto">
  <name>Task 2: Verify production server startup and SPA serving</name>
  <files></files>
  <action>
After build completes (Task 1), verify the full production path works:

1. Run `npm run build` to ensure clean dist exists
2. Start the production server: `NODE_ENV=production node --env-file=.env packages/server/dist/index.js &`
3. Wait 2 seconds for startup
4. Verify health endpoint: `curl -s http://localhost:3001/health` should return `{"status":"ok"}`
5. Verify SPA serving: `curl -s http://localhost:3001/` should return HTML containing the React app mount point
6. Verify SPA catch-all: `curl -s http://localhost:3001/some/random/route` should return the same index.html (not a 404)
7. Verify tRPC endpoint exists: `curl -s http://localhost:3001/trpc` should NOT return the SPA (should return a tRPC error or empty response)
8. Kill the server process

This is a verification-only task — no files are modified. If any check fails, investigate and report the issue but do not attempt fixes outside plan scope.

Note: The server listens on port 3001. Make sure no other server is running on that port before starting.
  </action>
  <verify>
    <automated>npm run build && timeout 10 node --env-file=.env packages/server/dist/index.js & sleep 2 && curl -sf http://localhost:3001/health | grep -q '"ok"' && curl -sf http://localhost:3001/ | grep -q 'html' && kill %1 2>/dev/null && echo "PASS" || (kill %1 2>/dev/null; echo "FAIL")</automated>
  </verify>
  <done>Production server starts from compiled JS, serves SPA at root, returns index.html for client-side routes, and health endpoint responds correctly. No dotenv dependency is used — environment loads via --env-file flag.</done>
</task>

</tasks>

<verification>
- `npm run build` completes without errors
- No .test.js or .test.d.ts files exist in packages/server/dist/
- Production server starts and responds on port 3001
- GET / returns React SPA HTML
- GET /health returns {"status":"ok"}
- GET /any/client/route returns index.html (SPA catch-all)
- No dotenv in production dependencies
</verification>

<success_criteria>
Build pipeline produces clean output and the compiled server correctly serves the SPA in production mode with environment variables loaded via Node 20 native --env-file flag.
</success_criteria>

<output>
After completion, create `.planning/phases/18-production-build-and-directory-layout/18-01-SUMMARY.md`
</output>
