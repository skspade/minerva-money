---
phase: 07-budget-engine
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - packages/server/migrations/005-budget-funding-step.sql
  - packages/server/src/budget/budget-service.ts
  - packages/server/src/budget/budget-service.test.ts
autonomous: true
requirements:
  - BUDG-02
  - BUDG-05
  - BUDG-07
must_haves:
  truths:
    - "Default allocation can be set for any category and persists"
    - "Monthly allocation can be set and retrieved for a category/period pair"
    - "Manual override of an allocation persists (upsert behavior)"
    - "Spending per category per month is computed correctly excluding confirmed transfers"
    - "Split transactions attribute spending to split categories, not parent category"
  artifacts:
    - path: "packages/server/migrations/005-budget-funding-step.sql"
      provides: "funding_step column on budget_allocations"
      contains: "funding_step"
    - path: "packages/server/src/budget/budget-service.ts"
      provides: "Budget service functions"
      exports: ["setDefaultAllocation", "getDefaults", "deleteDefault", "setAllocation", "getAllocation", "getSpentForCategory"]
    - path: "packages/server/src/budget/budget-service.test.ts"
      provides: "TDD tests for budget service"
      min_lines: 80
  key_links:
    - from: "packages/server/src/budget/budget-service.ts"
      to: "budget_allocations table"
      via: "better-sqlite3 prepared statements"
      pattern: "budget_allocations"
---

<objective>
Build the budget service foundation with migration, default allocation CRUD, monthly allocation CRUD, and per-category spending computation.

Purpose: Establishes the core data layer that all other budget plans build on -- defaults for auto-funding, allocations for manual overrides, and spending queries for budget summary.
Output: Working budget-service.ts with TDD tests, migration 005
</objective>

<execution_context>
@/Users/seanspade/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seanspade/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-budget-engine/07-CONTEXT.md
@.planning/phases/07-budget-engine/07-RESEARCH.md
@packages/server/migrations/001-initial-schema.sql
@packages/server/src/categories/category-service.ts
@packages/server/src/transfers/transfer-service.ts

<interfaces>
<!-- Existing budget_allocations table (from 001-initial-schema.sql): -->
```sql
CREATE TABLE budget_allocations (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  category_id INTEGER NOT NULL REFERENCES categories(id) ON DELETE CASCADE,
  period TEXT NOT NULL,
  amount INTEGER NOT NULL DEFAULT 0,
  is_default INTEGER NOT NULL DEFAULT 0,
  created_at TEXT NOT NULL DEFAULT (datetime('now')),
  updated_at TEXT NOT NULL DEFAULT (datetime('now')),
  UNIQUE(category_id, period)
);
```

<!-- Service function pattern (from category-service.ts): -->
```typescript
import type Database from 'better-sqlite3';
export function listGroupsWithCategories(db: Database.Database): CategoryGroup[] { ... }
```

<!-- Confirmed transfer detection pattern (from trpc-router.ts lines 183-187): -->
```sql
CASE WHEN EXISTS (
  SELECT 1 FROM transfer_links tl
  WHERE (tl.transaction_a_id = t.id OR tl.transaction_b_id = t.id)
  AND tl.confirmed = 1
) THEN 1 ELSE 0 END AS is_transfer
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migration and default allocation CRUD with TDD</name>
  <files>packages/server/migrations/005-budget-funding-step.sql, packages/server/src/budget/budget-service.ts, packages/server/src/budget/budget-service.test.ts</files>
  <behavior>
    - setDefaultAllocation(db, categoryId, amount) upserts a row with is_default=1, period='default'
    - setDefaultAllocation called twice for same category updates the amount (not duplicate)
    - getDefaults(db) returns all default allocations as { categoryId, amount }[]
    - getDefaults returns empty array when no defaults set
    - deleteDefault(db, categoryId) removes the default row
    - deleteDefault for nonexistent default does not throw
    - All amounts are integer cents
  </behavior>
  <action>
    1. Create migration `005-budget-funding-step.sql`: `ALTER TABLE budget_allocations ADD COLUMN funding_step INTEGER NOT NULL DEFAULT 0;`
    2. Create `packages/server/src/budget/budget-service.ts` with:
       - `setDefaultAllocation(db, categoryId, amount)`: INSERT INTO budget_allocations (category_id, period, amount, is_default) VALUES (?, 'default', ?, 1) ON CONFLICT(category_id, period) DO UPDATE SET amount = excluded.amount, updated_at = datetime('now')
       - `getDefaults(db)`: SELECT category_id, amount FROM budget_allocations WHERE is_default = 1 AND period = 'default', return mapped to { categoryId, amount }[]
       - `deleteDefault(db, categoryId)`: DELETE FROM budget_allocations WHERE category_id = ? AND period = 'default' AND is_default = 1
    3. Create test file using createDatabase() from db/connection.ts for test setup. Insert test categories via direct SQL in beforeEach. Follow RED-GREEN-REFACTOR.
  </action>
  <verify>
    <automated>cd packages/server && npx vitest run src/budget/budget-service.test.ts</automated>
  </verify>
  <done>Default allocation CRUD works with upsert semantics, migration adds funding_step column</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Monthly allocation CRUD and spending computation with TDD</name>
  <files>packages/server/src/budget/budget-service.ts, packages/server/src/budget/budget-service.test.ts</files>
  <behavior>
    - setAllocation(db, categoryId, period, amount) upserts a monthly allocation row with is_default=0
    - setAllocation overwrites existing allocation for same (categoryId, period) including auto-funded ones
    - getAllocation(db, categoryId, period) returns amount or 0 if not set
    - getSpentForCategory(db, categoryId, period) returns sum of negative transaction amounts for that category in that month as positive cents
    - getSpentForCategory excludes confirmed transfers
    - getSpentForCategory attributes split transaction amounts to the split's category, not the parent
    - getSpentForCategory returns 0 when no transactions exist
  </behavior>
  <action>
    1. Add to budget-service.ts:
       - `setAllocation(db, categoryId, period, amount)`: INSERT INTO budget_allocations (category_id, period, amount, is_default) VALUES (?, ?, ?, 0) ON CONFLICT(category_id, period) DO UPDATE SET amount = excluded.amount, updated_at = datetime('now')
       - `getAllocation(db, categoryId, period)`: SELECT amount FROM budget_allocations WHERE category_id = ? AND period = ? AND is_default = 0, return amount or 0
       - `getSpentForCategory(db, categoryId, period)`: Query transactions for the month. For unsplit transactions with matching category_id, sum their amounts. For split transactions, sum from transaction_splits where category_id matches. Exclude confirmed transfers via NOT EXISTS on transfer_links. Return absolute value (spending as positive cents).
    2. Spending query must handle the date range correctly: period "2026-03" means dates from "2026-03-01" through "2026-03-31". Use `t.date >= ? AND t.date < ?` with start of current month and start of next month for correctness across all month lengths.
    3. Tests: insert test transactions (some with splits, some transfers) and verify spending computation.
  </action>
  <verify>
    <automated>cd packages/server && npx vitest run src/budget/budget-service.test.ts</automated>
  </verify>
  <done>Monthly allocations support upsert, spending computation correctly handles splits and transfer exclusion</done>
</task>

</tasks>

<verification>
- All tests pass: `cd packages/server && npx vitest run src/budget/budget-service.test.ts`
- Migration 005 exists and adds funding_step column
- budget-service.ts exports all required functions
- Spending computation excludes confirmed transfers and handles splits
</verification>

<success_criteria>
- Default allocation CRUD with upsert semantics works
- Monthly allocation CRUD with manual override (upsert) works
- Spending per category computed correctly with transfer/split handling
- All budget math uses integer cents
- Migration 005 applied successfully
</success_criteria>

<output>
After completion, create `.planning/phases/07-budget-engine/07-01-SUMMARY.md`
</output>
