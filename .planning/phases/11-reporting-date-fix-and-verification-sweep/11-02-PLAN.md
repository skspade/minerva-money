---
phase: 11-reporting-date-fix-and-verification-sweep
plan: 02
type: execute
wave: 2
depends_on: [11-01]
files_modified:
  - .planning/phases/05-categorization-rules-engine/VERIFICATION.md
  - .planning/phases/06-transfer-detection/VERIFICATION.md
  - .planning/phases/09-dashboard-and-reporting/VERIFICATION.md
autonomous: true
requirements:
  - CATG-02
  - CATG-03
  - CATG-04
  - CATG-05
  - CATG-07
  - CATG-08
  - CATG-09
  - REPT-03

must_haves:
  truths:
    - "Phase 5 VERIFICATION.md confirms CATG-02 through CATG-05 are satisfied"
    - "Phase 6 VERIFICATION.md confirms CATG-07 through CATG-09 are satisfied"
    - "Phase 9 VERIFICATION.md confirms REPT-01 through REPT-03 are satisfied"
    - "All verification documents cite specific file paths and line numbers"
    - "All verification documents reference passing test evidence"
  artifacts:
    - path: ".planning/phases/05-categorization-rules-engine/VERIFICATION.md"
      provides: "Phase 5 requirement verification"
      contains: "CATG-02"
    - path: ".planning/phases/06-transfer-detection/VERIFICATION.md"
      provides: "Phase 6 requirement verification"
      contains: "CATG-07"
    - path: ".planning/phases/09-dashboard-and-reporting/VERIFICATION.md"
      provides: "Phase 9 requirement verification"
      contains: "REPT-01"
  key_links:
    - from: ".planning/phases/05-categorization-rules-engine/VERIFICATION.md"
      to: "packages/server/src/rules/rules-service.ts"
      via: "file path and line number references"
      pattern: "rules-service"
    - from: ".planning/phases/06-transfer-detection/VERIFICATION.md"
      to: "packages/server/src/transfers/transfer-service.ts"
      via: "file path and line number references"
      pattern: "transfer-service"
    - from: ".planning/phases/09-dashboard-and-reporting/VERIFICATION.md"
      to: "packages/server/src/reports/reports-service.ts"
      via: "file path and line number references"
      pattern: "reports-service"
---

<objective>
Create three VERIFICATION.md documents confirming all 10 requirements are satisfied.

Purpose: Formally close the verification gap for Phases 5, 6, and 9 by documenting that each requirement has working code and test evidence in the codebase.

Output: Three VERIFICATION.md files in their respective phase directories.
</objective>

<execution_context>
@/Users/seanspade/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seanspade/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/VERIFICATION.md
@packages/server/src/rules/rules-service.ts
@packages/server/src/transfers/transfer-service.ts
@packages/server/src/reports/reports-service.ts
@packages/server/src/sync/trpc-router.ts
</context>

<interfaces>
<!-- Phase 1 VERIFICATION.md format (the template to follow) -->
Structure:
  # Phase N: Name - Verification
  **Verified:** [date]
  **Phase Goal:** [from roadmap]
  **Result:** PASS

  ## Requirements
  ### REQ-ID: Description
  **Status:** PASS
  **Evidence:**
  - [file path + line number references to implementation]

  ## Test Evidence
  - [test file]: N tests passing — covers [areas]

  ## Summary
  [Paragraph confirming all requirements satisfied]
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create Phase 5, 6, and 9 VERIFICATION.md documents</name>
  <files>.planning/phases/05-categorization-rules-engine/VERIFICATION.md, .planning/phases/06-transfer-detection/VERIFICATION.md, .planning/phases/09-dashboard-and-reporting/VERIFICATION.md</files>
  <action>
    First, run the full test suite to get current test counts:
    - `npx vitest run packages/server/src/rules/rules-service.test.ts`
    - `npx vitest run packages/server/src/transfers/transfer-service.test.ts`
    - `npx vitest run packages/server/src/reports/reports-service.test.ts`

    Then read the source files to get current line numbers (they may have shifted after Plan 11-01):
    - `packages/server/src/rules/rules-service.ts`
    - `packages/server/src/transfers/transfer-service.ts`
    - `packages/server/src/reports/reports-service.ts`
    - `packages/server/src/sync/trpc-router.ts`

    Read the Phase 1 VERIFICATION.md at `.planning/phases/01-foundation/VERIFICATION.md` for format reference.

    Create three VERIFICATION.md files following the Phase 1 format exactly:

    **Phase 5 VERIFICATION.md** (`.planning/phases/05-categorization-rules-engine/VERIFICATION.md`):
    - Phase Goal: from ROADMAP.md Phase 5 entry
    - CATG-02: Evidence from `createRule`, `matchesRule` in rules-service.ts with line numbers. Merchant pattern matching (exact/contains), amount range (min/max), memo pattern.
    - CATG-03: Evidence from `previewRule` and `applyRule` in rules-service.ts with line numbers. Shows retroactive application to existing transactions.
    - CATG-04: Evidence from `categorizeNewTransactions` in rules-service.ts with line numbers. Also reference its call site in sync-service.ts.
    - CATG-05: Evidence from `computeSpecificity` scoring logic and `ORDER BY specificity_score DESC, id DESC` in rules-service.ts. Show exact scoring: exact merchant=3, contains=2, amount range=2/1, memo=1. Newer wins ties via `id DESC`.
    - Test Evidence: rules-service.test.ts test count and coverage areas

    **Phase 6 VERIFICATION.md** (`.planning/phases/06-transfer-detection/VERIFICATION.md`):
    - Phase Goal: from ROADMAP.md Phase 6 entry
    - CATG-07: Evidence from `detectTransferCandidates` in transfer-service.ts with line numbers. Matching logic: offsetting amounts, different accounts, date window, not pending, not already linked.
    - CATG-08: Evidence from `confirmTransfer` and `manuallyLinkTransfer` in transfer-service.ts with line numbers.
    - CATG-09: Evidence from `NOT EXISTS (SELECT 1 FROM transfer_links ...)` clauses in reports-service.ts with line numbers. Reference all four exclusion clauses in getSpendingByCategory and getSpendingOverTime.
    - Test Evidence: transfer-service.test.ts test count and coverage areas

    **Phase 9 VERIFICATION.md** (`.planning/phases/09-dashboard-and-reporting/VERIFICATION.md`):
    - Phase Goal: from ROADMAP.md Phase 9 entry
    - REPT-01: Evidence from `getSpendingByCategory` in reports-service.ts with line numbers + tRPC `reports.spendingByCategory` in trpc-router.ts with line numbers. Note: date filtering via startDate/endDate parameters.
    - REPT-02: Evidence from `getSpendingOverTime` in reports-service.ts with line numbers + tRPC `reports.spendingOverTime` in trpc-router.ts with line numbers. Note: monthly aggregation via strftime.
    - REPT-03: Evidence from `getNetWorth` in reports-service.ts with line numbers + tRPC `reports.netWorth` in trpc-router.ts with line numbers.
    - Test Evidence: reports-service.test.ts test count and coverage areas
    - Note: Reference the date fix from Plan 11-01 as ensuring correct boundary behavior.
  </action>
  <verify>
    <automated>ls .planning/phases/05-categorization-rules-engine/VERIFICATION.md .planning/phases/06-transfer-detection/VERIFICATION.md .planning/phases/09-dashboard-and-reporting/VERIFICATION.md</automated>
  </verify>
  <done>Three VERIFICATION.md files exist with PASS status for all 10 requirements, each citing specific file paths and line numbers as evidence</done>
</task>

</tasks>

<verification>
- All three VERIFICATION.md files exist in their respective phase directories
- Each file follows Phase 1 VERIFICATION.md format
- Phase 5 verification covers CATG-02, CATG-03, CATG-04, CATG-05
- Phase 6 verification covers CATG-07, CATG-08, CATG-09
- Phase 9 verification covers REPT-01, REPT-02, REPT-03
- All requirements show PASS status
- Evidence cites specific file paths and line numbers
</verification>

<success_criteria>
- Three VERIFICATION.md documents created
- All 10 requirements verified with PASS status
- Evidence references current file paths and line numbers
- Test evidence sections reference actual test counts
</success_criteria>

<output>
After completion, create `.planning/phases/11-reporting-date-fix-and-verification-sweep/11-02-SUMMARY.md`
</output>
