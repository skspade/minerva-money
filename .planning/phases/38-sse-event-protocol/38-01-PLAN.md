---
phase: 38-sse-event-protocol
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared/src/sse-events.ts
  - packages/shared/src/types.ts
  - packages/shared/src/sse-events.test.ts
autonomous: true
requirements:
  - PROTO-01
  - PROTO-02
  - PROTO-03

must_haves:
  truths:
    - "A discriminated union type SSEEvent defines all 6 event kinds: session, text-delta, tool-start, tool-end, done, error"
    - "The done event type has a text field containing the full assembled response"
    - "The error event type has a message field and an optional partialText field"
    - "Both packages/server and packages/client can import SSE event types from @minerva/shared"
  artifacts:
    - path: "packages/shared/src/sse-events.ts"
      provides: "SSE event type definitions"
      exports: ["SSESessionEvent", "SSETextDeltaEvent", "SSEToolStartEvent", "SSEToolEndEvent", "SSEDoneEvent", "SSEErrorEvent", "SSEEvent", "SSEEventType"]
    - path: "packages/shared/src/types.ts"
      provides: "Re-exports SSE event types"
      contains: "export.*from.*sse-events"
  key_links:
    - from: "packages/shared/src/types.ts"
      to: "packages/shared/src/sse-events.ts"
      via: "re-export"
      pattern: "export.*from.*sse-events"
---

<objective>
Define the SSE event type contract in the shared package so server and client share compile-time types for all streaming events.

Purpose: Prevent protocol drift between server-emitted and client-consumed SSE events by defining a single source of truth as TypeScript discriminated union types.
Output: `packages/shared/src/sse-events.ts` with all 6 event interfaces + union type, re-exported from `types.ts`.
</objective>

<execution_context>
@/Users/seanspade/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seanspade/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@packages/shared/src/types.ts
@packages/shared/package.json
@packages/shared/tsconfig.json

<interfaces>
<!-- Current shared package exports — executor needs to know what exists to add re-exports -->

From packages/shared/src/types.ts:
```typescript
export type Cents = number & { readonly __brand: 'Cents' };
export function toCents(dollars: number): Cents;
```

From packages/shared/package.json:
```json
{
  "main": "./dist/types.js",
  "types": "./dist/types.d.ts"
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define SSE event types and verify with tests</name>
  <files>packages/shared/src/sse-events.ts, packages/shared/src/sse-events.test.ts</files>
  <behavior>
    - Test 1: SSEEvent union type accepts a valid session event object { type: 'session', sessionId: 'abc' }
    - Test 2: SSEEvent union type accepts a valid text-delta event object { type: 'text-delta', text: 'hello' }
    - Test 3: SSEEvent union type accepts a valid tool-start event object { type: 'tool-start', tool: 'get_budget' }
    - Test 4: SSEEvent union type accepts a valid tool-end event object { type: 'tool-end', tool: 'get_budget' }
    - Test 5: SSEEvent union type accepts a valid done event object { type: 'done', text: 'Full response here' }
    - Test 6: SSEEvent union type accepts a valid error event object { type: 'error', message: 'Something went wrong' }
    - Test 7: SSEEvent union type accepts a valid error event with optional partialText { type: 'error', message: 'Failed', partialText: 'Partial...' }
    - Test 8: SSEEventType literal union contains all 6 type strings
    - Test 9: Exhaustive switch on event.type narrows to correct fields (e.g., switching on 'done' gives access to .text)
  </behavior>
  <action>
    Create `packages/shared/src/sse-events.ts` with:

    1. Six individual event interfaces, each with a `type` string literal discriminant:
       - `SSESessionEvent` — `{ type: 'session'; sessionId: string }`
       - `SSETextDeltaEvent` — `{ type: 'text-delta'; text: string }`
       - `SSEToolStartEvent` — `{ type: 'tool-start'; tool: string }`
       - `SSEToolEndEvent` — `{ type: 'tool-end'; tool: string }`
       - `SSEDoneEvent` — `{ type: 'done'; text: string }`
       - `SSEErrorEvent` — `{ type: 'error'; message: string; partialText?: string }`

    2. A discriminated union type:
       `type SSEEvent = SSESessionEvent | SSETextDeltaEvent | SSEToolStartEvent | SSEToolEndEvent | SSEDoneEvent | SSEErrorEvent`

    3. A string literal union:
       `type SSEEventType = SSEEvent['type']`
       (This extracts 'session' | 'text-delta' | 'tool-start' | 'tool-end' | 'done' | 'error' automatically)

    4. Add JSDoc comments on each interface explaining its purpose and when it's emitted.

    5. Export all interfaces, the union type, and the event type literal.

    Then create `packages/shared/src/sse-events.test.ts` with vitest tests that:
    - Use `expectTypeOf` from vitest to verify type relationships
    - Create typed objects for each event kind and assert they satisfy SSEEvent
    - Verify SSEEventType contains exactly the 6 expected strings
    - Verify exhaustive type narrowing works in a switch statement
    - Verify the done event's text field and error event's message + optional partialText fields

    Follow TDD: write the test file first with the expected imports, run tests (RED), then create sse-events.ts (GREEN).
  </action>
  <verify>
    <automated>cd /Users/seanspade/Documents/Source/minerva-money && npx vitest run packages/shared/src/sse-events.test.ts</automated>
  </verify>
  <done>
    - sse-events.ts exports 6 individual event interfaces, SSEEvent union, and SSEEventType literal union
    - All 9+ tests pass confirming type correctness
    - Each event interface has a `type` string literal discriminant
    - SSEDoneEvent has `text: string` field
    - SSEErrorEvent has `message: string` and `partialText?: string` fields
  </done>
</task>

<task type="auto">
  <name>Task 2: Re-export from types.ts and verify cross-package imports</name>
  <files>packages/shared/src/types.ts</files>
  <action>
    1. Add a re-export line to `packages/shared/src/types.ts`:
       ```typescript
       export type { SSESessionEvent, SSETextDeltaEvent, SSEToolStartEvent, SSEToolEndEvent, SSEDoneEvent, SSEErrorEvent, SSEEvent, SSEEventType } from './sse-events.js';
       ```
       Use `.js` extension per ESM convention (TypeScript resolves .js to .ts during compilation).
       Use `export type` since these are all type-only exports (no runtime values).

    2. Build the shared package to confirm compilation:
       ```bash
       cd packages/shared && npm run build
       ```

    3. Verify the built output contains the SSE event types:
       ```bash
       grep "SSEEvent" packages/shared/dist/types.d.ts
       ```

    4. Verify cross-package import works by running the full project build:
       ```bash
       npm run build
       ```
       This builds shared first, then server, then client — confirming both downstream packages can resolve @minerva/shared types.
  </action>
  <verify>
    <automated>cd /Users/seanspade/Documents/Source/minerva-money && npm run build</automated>
  </verify>
  <done>
    - types.ts re-exports all SSE event types from sse-events.ts
    - `npm run build` succeeds across all three packages
    - packages/shared/dist/types.d.ts contains SSEEvent type declarations
    - Server and client packages compile without errors (proving @minerva/shared import path works)
  </done>
</task>

</tasks>

<verification>
1. `npx vitest run packages/shared/src/sse-events.test.ts` — all type tests pass
2. `npm run build` — full monorepo build succeeds
3. `grep "SSEEvent" packages/shared/dist/types.d.ts` — confirms types in build output
4. `grep "SSEEvent" packages/shared/dist/sse-events.d.ts` — confirms declaration file generated
</verification>

<success_criteria>
- SSEEvent discriminated union type defines all 6 event kinds (session, text-delta, tool-start, tool-end, done, error) — PROTO-01
- done event has `text: string` field — PROTO-02
- error event has `message: string` and `partialText?: string` fields — PROTO-03
- Both server and client compile successfully with @minerva/shared in their dependencies
- All vitest tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/38-sse-event-protocol/38-01-SUMMARY.md`
</output>
