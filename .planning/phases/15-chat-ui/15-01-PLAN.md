---
phase: 15-chat-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/client/package.json
  - packages/client/src/styles/app.css
  - packages/client/src/components/Layout.tsx
  - packages/client/src/app.tsx
  - packages/client/src/pages/ChatPage.tsx
autonomous: true
requirements:
  - UI-01
  - UI-05

must_haves:
  truths:
    - "User can navigate to /chat from the nav bar"
    - "Chat page renders with full-height layout below the nav bar"
    - "Chat page has a scrollable message area and a fixed input bar at the bottom"
    - "Empty state shows a welcome message with example questions"
  artifacts:
    - path: "packages/client/src/pages/ChatPage.tsx"
      provides: "Chat page component with full-height layout, message list, and input bar"
      min_lines: 40
    - path: "packages/client/src/styles/app.css"
      provides: "Tailwind typography plugin registration"
      contains: "@plugin"
  key_links:
    - from: "packages/client/src/app.tsx"
      to: "packages/client/src/pages/ChatPage.tsx"
      via: "Route path='chat'"
      pattern: "path.*chat"
    - from: "packages/client/src/components/Layout.tsx"
      to: "/chat"
      via: "NavLink to='/chat'"
      pattern: "NavLink.*chat"
---

<objective>
Install chat UI dependencies, add the /chat route and navigation link, and create the ChatPage component shell with full-height layout, empty state, and input bar.

Purpose: Establish the page structure, routing, and navigation so the chat page is reachable and has the correct layout before adding message interaction logic.
Output: A navigable /chat page with full-height two-region layout (scrollable message area + fixed input bar), empty state welcome message, and typography plugin ready.
</objective>

<execution_context>
@/Users/seanspade/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seanspade/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@packages/client/src/app.tsx
@packages/client/src/components/Layout.tsx
@packages/client/src/styles/app.css
@packages/client/package.json

<interfaces>
<!-- Key types and contracts the executor needs. -->

From packages/server/src/agent/agent-router.ts:
```typescript
// agent.chat mutation: input { message: string, sessionId?: string }
// returns: { response: string, sessionId: string }
```

From packages/client/src/trpc.ts:
```typescript
export const { TRPCProvider, useTRPC } = createTRPCContext<AppRouter>();
```

From packages/client/src/components/Layout.tsx (NavLink pattern):
```typescript
<NavLink
  to="/budget"
  className={({ isActive }) =>
    `px-3 py-1 rounded ${isActive ? 'bg-gray-700' : 'hover:bg-gray-800'}`
  }
>
  Budget
</NavLink>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies and configure typography plugin</name>
  <files>packages/client/package.json, packages/client/src/styles/app.css</files>
  <action>
1. Install dependencies in packages/client:
   ```bash
   cd packages/client && npm install react-markdown remark-gfm @tailwindcss/typography
   ```

2. Add the typography plugin to `packages/client/src/styles/app.css`. Since this project uses Tailwind CSS v4 with CSS-based config, add the `@plugin` directive AFTER the existing `@import "tailwindcss"` line:
   ```css
   @import "tailwindcss";
   @plugin "@tailwindcss/typography";
   ```
  </action>
  <verify>
    <automated>cd packages/client && node -e "require('react-markdown'); require('remark-gfm')" && grep '@plugin.*typography' src/styles/app.css</automated>
  </verify>
  <done>react-markdown, remark-gfm, and @tailwindcss/typography are installed. Typography plugin is registered in app.css via @plugin directive.</done>
</task>

<task type="auto">
  <name>Task 2: Add chat route, nav link, and ChatPage shell</name>
  <files>packages/client/src/pages/ChatPage.tsx, packages/client/src/app.tsx, packages/client/src/components/Layout.tsx</files>
  <action>
1. Create `packages/client/src/pages/ChatPage.tsx` with a full-height two-region layout:

   - The page must fill the remaining viewport height below the nav bar. The nav bar is ~56px tall (`py-3` with text = roughly 56px). Use `h-[calc(100vh-56px)]` with `flex flex-col` on the outermost container.
   - Do NOT wrap in the usual `max-w-6xl` — chat uses full width (per CONTEXT.md decision).
   - Top region: scrollable message list area using `flex-1 overflow-y-auto`. For now, render an empty state with:
     - A welcome heading: "Ask Minerva anything about your finances"
     - 3-4 example question buttons the user can click to populate the input field. Examples: "What's my account balance?", "How much did I spend on groceries this month?", "Show my budget summary", "Any uncategorized transactions?"
     - Clicking an example question should set the input field value to that question text (use local state).
   - Bottom region: fixed input bar pinned to the bottom with `border-t` separator. Contains:
     - A textarea (not single-line input — allows multi-line) with placeholder "Ask about your finances..."
     - A Send button to the right of the textarea
     - Use `flex` layout for the input row with the textarea taking remaining space
     - Enter to send (call a stub `handleSend` function that does nothing yet), Shift+Enter for newline. Prevent default on Enter without Shift.

   - Export as default: `export default function ChatPage()`

2. Add the chat route to `packages/client/src/app.tsx`:
   - Import ChatPage: `import ChatPage from './pages/ChatPage';`
   - Add route inside the `<Route element={<Layout />}>` block: `<Route path="chat" element={<ChatPage />} />`

3. Add "Chat" NavLink to `packages/client/src/components/Layout.tsx`:
   - Add a NavLink to `/chat` following the exact same pattern as existing NavLinks (same className function with isActive check).
   - Place it AFTER the "Reports" NavLink (last in the list, before the closing `</div>` of the nav links group).

4. The Layout.tsx renders `<Outlet />` inside `<main className="mx-auto max-w-6xl px-4 py-6">`. The ChatPage needs full width. Handle this by having ChatPage use absolute/fixed positioning or negative margins to break out of the max-w-6xl constraint, OR better: update Layout.tsx so the main element does NOT apply max-w-6xl and padding when on the chat route. Simplest approach: have ChatPage manage its own full-height layout within the Outlet, and override the main padding. Use a wrapper div in ChatPage with `h-[calc(100vh-56px)] -mx-4 -mt-6` to negate the main element's padding, then the chat content fills full width.
  </action>
  <verify>
    <automated>cd packages/client && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
- /chat route exists in app.tsx
- "Chat" NavLink appears in Layout.tsx nav bar with same isActive styling
- ChatPage renders with full-height layout: scrollable message area on top, fixed input bar on bottom
- Empty state shows welcome message with clickable example questions
- Textarea supports Enter to send, Shift+Enter for newline
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with no type errors
2. Navigate to /chat in the browser — page renders with full height, no scrollbar on the page itself
3. Nav bar shows "Chat" link with active state highlighting when on /chat
4. Empty state shows welcome message and example questions
5. Clicking an example question fills the textarea
6. Textarea allows multi-line input with Shift+Enter
</verification>

<success_criteria>
- Chat page is accessible at /chat via nav bar link
- Full-height layout fills the viewport below the nav bar
- Message area is scrollable, input bar is fixed at the bottom
- Empty state displays welcome message with example questions
- Typography plugin is registered and ready for markdown rendering
</success_criteria>

<output>
After completion, create `.planning/phases/15-chat-ui/15-01-SUMMARY.md`
</output>
