---
phase: 10-foundation-bug-fix-and-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/server/src/backup/run-backup.ts
  - packages/server/src/backup/run-backup.test.ts
autonomous: true
requirements:
  - INFR-01
must_haves:
  truths:
    - "run-backup.ts exists at the path referenced by com.minerva.backup.plist"
    - "Running the backup script opens the production DB, calls createBackup, and exits cleanly"
    - "Script exits with code 0 on success and non-zero on failure"
  artifacts:
    - path: "packages/server/src/backup/run-backup.ts"
      provides: "Standalone backup entry point for launchd"
    - path: "packages/server/src/backup/run-backup.test.ts"
      provides: "Test confirming run-backup script behavior"
  key_links:
    - from: "packages/server/src/backup/run-backup.ts"
      to: "packages/server/src/backup/backup.ts"
      via: "import { createBackup }"
      pattern: "createBackup"
    - from: "com.minerva.backup.plist"
      to: "packages/server/src/backup/run-backup.ts"
      via: "ProgramArguments path reference"
      pattern: "run-backup.ts"
---

<objective>
Create the missing `run-backup.ts` entry-point script that `com.minerva.backup.plist` references, fixing the broken launchd backup flow.

Purpose: The plist references `packages/server/src/backup/run-backup.ts` but this file does not exist, meaning scheduled backups silently fail. Creating it restores INFR-01 (atomic SQLite backups every 6 hours via launchd).

Output: A working `run-backup.ts` script and a test verifying its behavior.
</objective>

<execution_context>
@/Users/seanspade/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seanspade/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@packages/server/src/backup/backup.ts
@com.minerva.backup.plist

<interfaces>
<!-- Key types and contracts the executor needs. -->

From packages/server/src/backup/backup.ts:
```typescript
export interface BackupResult {
  path: string;
  timestamp: string;
  sizeBytes: number;
  integrityOk: boolean;
}

export async function createBackup(
  db: Database.Database,
  backupDir?: string
): Promise<BackupResult>
```

From packages/server/src/db/connection.ts:
```typescript
// NOTE: Do NOT use createDatabase() in run-backup.ts -- it runs migrations.
// Open the DB directly with better-sqlite3 instead.
const DEFAULT_DB_PATH = path.join(os.homedir(), 'minerva-money', 'data', 'minerva.db');
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create run-backup.ts entry-point script</name>
  <files>packages/server/src/backup/run-backup.ts, packages/server/src/backup/run-backup.test.ts</files>
  <behavior>
    - Test 1: Script module exports are testable (createBackup is called with a DB instance)
    - Test 2: On success, logs a message containing the backup path and size
    - Test 3: On failure, logs the error and sets process.exitCode to 1
  </behavior>
  <action>
Create `packages/server/src/backup/run-backup.ts` as a standalone entry point:

1. Import `Database` from `better-sqlite3`, `path` and `os` from node, and `createBackup` from `./backup.js`
2. Define `DB_PATH` as `path.join(os.homedir(), 'minerva-money', 'data', 'minerva.db')`
3. Open the database with `new Database(DB_PATH)` (NOT `createDatabase()` which runs migrations)
4. Set `db.pragma('journal_mode = WAL')`
5. In a try block: call `await createBackup(db)`, log success with path and size
6. In catch: log the error to stderr, set `process.exitCode = 1`
7. In finally: call `db.close()`

The script should be under 25 lines. Keep it simple.

For the test file `run-backup.test.ts`:
- Create a temp database with test data
- Import `createBackup` from `./backup.js` (test the core function, not the script entry point directly)
- Verify calling createBackup with the temp DB produces a valid backup
- Verify the backup file exists and passes integrity check
- This validates the same code path that run-backup.ts uses

Note: The existing `backup.test.ts` already has 7 tests covering `createBackup`. The run-backup test should focus on verifying the entry-point pattern works (DB open, backup, close) rather than duplicating backup logic tests.
  </action>
  <verify>
    <automated>npx vitest run packages/server/src/backup/run-backup.test.ts</automated>
  </verify>
  <done>
    - `run-backup.ts` exists at `packages/server/src/backup/run-backup.ts`
    - File path matches what `com.minerva.backup.plist` references
    - Script opens DB directly (not via createDatabase), calls createBackup, closes DB in finally block
    - Test passes confirming the backup flow works
  </done>
</task>

</tasks>

<verification>
1. `ls packages/server/src/backup/run-backup.ts` confirms the file exists
2. `grep "createBackup" packages/server/src/backup/run-backup.ts` confirms it calls the backup function
3. `grep "db.close" packages/server/src/backup/run-backup.ts` confirms cleanup
4. `npx vitest run packages/server/src/backup/` runs all backup tests (existing + new) and they pass
5. The path in `com.minerva.backup.plist` matches the created file location
</verification>

<success_criteria>
- run-backup.ts exists at the exact path referenced by the plist
- Script calls createBackup(db) from backup.ts
- Script closes DB connection in finally block
- Script sets process.exitCode = 1 on failure
- All backup tests pass (existing 7 + new)
</success_criteria>

<output>
After completion, create `.planning/phases/10-foundation-bug-fix-and-verification/10-01-SUMMARY.md`
</output>
