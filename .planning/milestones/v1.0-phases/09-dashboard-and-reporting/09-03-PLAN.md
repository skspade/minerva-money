---
phase: 09-dashboard-and-reporting
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - packages/client/src/pages/ReportsPage.tsx
autonomous: true
requirements:
  - REPT-01
  - REPT-02
  - REPT-03
must_haves:
  truths:
    - "User can view spending by category as a pie chart with toggle to bar chart"
    - "User can select a date range for spending by category and spending over time"
    - "User can view spending over time as a line chart with month labels"
    - "User can view net worth trend as a line chart"
    - "All charts display currency values using formatCurrency"
  artifacts:
    - path: "packages/client/src/pages/ReportsPage.tsx"
      provides: "Full reporting page with three chart sections"
  key_links:
    - from: "packages/client/src/pages/ReportsPage.tsx"
      to: "tRPC reports.spendingByCategory"
      via: "useQuery with trpc.reports.spendingByCategory.queryOptions"
      pattern: "trpc\\.reports\\.spendingByCategory"
    - from: "packages/client/src/pages/ReportsPage.tsx"
      to: "tRPC reports.spendingOverTime"
      via: "useQuery"
      pattern: "trpc\\.reports\\.spendingOverTime"
    - from: "packages/client/src/pages/ReportsPage.tsx"
      to: "tRPC reports.netWorth"
      via: "useQuery"
      pattern: "trpc\\.reports\\.netWorth"
---

<objective>
Build the Reports page with three chart sections: spending by category (pie/bar toggle), spending over time (line), and net worth trend (line).

Purpose: Delivers REPT-01, REPT-02, REPT-03 with full charting and date range selection.
Output: ReportsPage.tsx with interactive charts using Recharts.
</objective>

<execution_context>
@/Users/seanspade/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seanspade/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@packages/client/src/pages/BudgetPage.tsx
@packages/client/src/lib/format.ts
@packages/client/src/trpc.ts

<interfaces>
From packages/client/src/trpc.ts:
```typescript
export const { TRPCProvider, useTRPC } = createTRPCContext<AppRouter>();
```

From packages/client/src/lib/format.ts:
```typescript
export function formatCurrency(cents: number): string;
```

tRPC query pattern (from BudgetPage.tsx):
```typescript
const trpc = useTRPC();
const { data, isLoading, error } = useQuery(
  trpc.budget.summary.queryOptions({ period }),
);
```

Reports tRPC endpoints (from Plan 09-02):
```typescript
trpc.reports.spendingByCategory.queryOptions({ startDate, endDate })
// returns { categoryId: number; categoryName: string; groupName: string; total: number }[]

trpc.reports.spendingOverTime.queryOptions({ startDate, endDate })
// returns { period: string; total: number }[]

trpc.reports.netWorth.queryOptions({ startDate?, endDate? })
// returns { date: string; total: number }[]
```

Recharts components needed:
```typescript
import { PieChart, Pie, Cell, BarChart, Bar, LineChart, Line, XAxis, YAxis, Tooltip, Legend, ResponsiveContainer, CartesianGrid } from 'recharts';
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Spending by category chart with pie/bar toggle and date range</name>
  <files>packages/client/src/pages/ReportsPage.tsx</files>
  <action>
    Create ReportsPage.tsx with:

    1. Date range state: startDate defaults to first day of current month (YYYY-MM-DD), endDate defaults to today (YYYY-MM-DD). Two native HTML date inputs for selection. Share this date range across spending-by-category and spending-over-time sections.

    2. Spending by Category section:
       - useQuery with trpc.reports.spendingByCategory.queryOptions({ startDate, endDate })
       - Toggle state between 'pie' and 'bar' (default: 'pie')
       - PieChart: ResponsiveContainer width="100%" height={350}, Pie with dataKey="total" nameKey="categoryName", Cell components with a color palette (use 8 distinct colors), Tooltip with formatCurrency formatter, Legend
       - BarChart: ResponsiveContainer width="100%" height={350}, CartesianGrid, XAxis dataKey="categoryName", YAxis with formatCurrency tick formatter, Bar dataKey="total" with Cell colors, Tooltip with formatCurrency
       - Toggle button: simple text button "Show as Bar/Pie"
       - Loading state: "Loading..." text
       - Empty state: "No spending data for this period"

    3. Spending Over Time section:
       - useQuery with trpc.reports.spendingOverTime.queryOptions({ startDate, endDate })
       - LineChart: ResponsiveContainer width="100%" height={300}, CartesianGrid, XAxis dataKey="period" (month labels), YAxis with formatCurrency tick formatter, Line dataKey="total" with stroke color, Tooltip with formatCurrency
       - Loading/empty states

    4. Net Worth Trend section:
       - useQuery with trpc.reports.netWorth.queryOptions({}) -- no date filter by default (show all data)
       - LineChart: ResponsiveContainer width="100%" height={300}, CartesianGrid, XAxis dataKey="date", YAxis with formatCurrency tick formatter, Line dataKey="total" with different stroke color, Tooltip with formatCurrency
       - Loading/empty states

    5. Page layout:
       - Title "Reports" with h2 heading matching BudgetPage style
       - Date range controls at top (shared between spending sections)
       - Three sections with white card containers (bg-white rounded-lg border border-gray-200 p-4) and section headings
       - Net worth section has its own card, no date filtering by default

    Use the same Tailwind patterns as BudgetPage (card containers, text sizing, spacing).
  </action>
  <verify>
    <automated>cd packages/client && npx tsc --noEmit</automated>
  </verify>
  <done>ReportsPage renders three chart sections: pie/bar toggle for spending by category, line chart for spending over time, line chart for net worth. Date range inputs control spending queries. All chart tooltips use formatCurrency.</done>
</task>

</tasks>

<verification>
- TypeScript compiles without errors
- ReportsPage imports and uses Recharts components correctly
- All three chart types render with proper data keys
- Date range inputs control spending queries
- formatCurrency used in all tooltips and axis formatters
</verification>

<success_criteria>
- Spending by category displays as pie chart with toggle to bar chart
- Spending over time displays as line chart with month labels
- Net worth displays as line chart with date labels
- Date range filter works for spending charts
- All values formatted as currency
</success_criteria>

<output>
After completion, create `.planning/phases/09-dashboard-and-reporting/09-03-SUMMARY.md`
</output>
