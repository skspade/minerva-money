---
phase: 11-reporting-date-fix-and-verification-sweep
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - packages/server/src/reports/reports-service.ts
  - packages/server/src/reports/reports-service.test.ts
autonomous: true
requirements:
  - REPT-01
  - REPT-02

must_haves:
  truths:
    - "getSpendingByCategory includes transactions on the endDate boundary"
    - "getSpendingOverTime includes transactions on the endDate boundary"
    - "All four SQL queries use <= for upper date bound"
    - "Existing tests continue to pass"
  artifacts:
    - path: "packages/server/src/reports/reports-service.ts"
      provides: "Fixed date boundary comparisons"
      contains: "t.date <= ?"
    - path: "packages/server/src/reports/reports-service.test.ts"
      provides: "Boundary date test cases"
  key_links:
    - from: "packages/server/src/reports/reports-service.ts"
      to: "SQL queries"
      via: "date comparison operators"
      pattern: "t\\.date <= \\?"
---

<objective>
Fix the off-by-one date boundary bug in spending report queries.

Purpose: `getSpendingByCategory` and `getSpendingOverTime` use `t.date < ?` for the upper bound, excluding transactions that fall exactly on the `endDate`. This means querying "today" misses today's transactions. The fix changes `<` to `<=` in all four SQL statements.

Output: Fixed reports-service.ts with inclusive date bounds and a new boundary test proving the fix.
</objective>

<execution_context>
@/Users/seanspade/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seanspade/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@packages/server/src/reports/reports-service.ts
@packages/server/src/reports/reports-service.test.ts
</context>

<feature>
  <name>Inclusive upper date bound for spending queries</name>
  <files>packages/server/src/reports/reports-service.ts, packages/server/src/reports/reports-service.test.ts</files>
  <behavior>
    - getSpendingByCategory includes a transaction whose date equals endDate
    - getSpendingOverTime includes a transaction whose date equals endDate
    - Existing tests continue to pass (they use endDate='2026-04-01' for March data, which works with both < and <=)

    Cases:
    - Insert transaction on '2026-03-15', query with endDate='2026-03-15' -> transaction IS included (currently excluded with <)
    - Insert transaction on '2026-03-15', query with endDate='2026-03-14' -> transaction NOT included
  </behavior>
  <implementation>
    RED: Add two test cases to reports-service.test.ts:
    1. In getSpendingByCategory describe block: "includes transactions on endDate boundary" — insert a transaction on '2026-03-15' with category, call getSpendingByCategory with startDate='2026-03-01' and endDate='2026-03-15', assert result includes the transaction.
    2. In getSpendingOverTime describe block: "includes transactions on endDate boundary" — insert a transaction on '2026-03-15', call getSpendingOverTime with startDate='2026-03-01' and endDate='2026-03-15', assert result includes the transaction's month.

    Run tests — both new tests MUST fail (proving the bug exists).

    GREEN: In reports-service.ts, change all four occurrences of `t.date < ?` to `t.date <= ?`:
    - Line 32: getSpendingByCategory unsplit query
    - Line 51: getSpendingByCategory split query
    - Line 98: getSpendingOverTime unsplit query
    - Line 114: getSpendingOverTime split query

    Run tests — all tests MUST pass (both new and existing).

    Note: Do NOT change getNetWorth — it already uses `<=` correctly on line 149.
  </implementation>
</feature>

<verification>
- `npx vitest run packages/server/src/reports/reports-service.test.ts` — all tests pass
- Grep confirms no remaining `t.date < ?` in spending queries: `grep "t.date < ?" packages/server/src/reports/reports-service.ts` returns no matches
- Grep confirms `t.date <= ?` appears 4 times in reports-service.ts (plus 1 in getNetWorth = 5 total)
</verification>

<success_criteria>
- All four `t.date < ?` in spending queries changed to `t.date <= ?`
- Two new boundary tests added and passing
- All existing tests continue to pass
- getNetWorth unchanged (already correct)
</success_criteria>

<output>
After completion, create `.planning/phases/11-reporting-date-fix-and-verification-sweep/11-01-SUMMARY.md`
</output>
