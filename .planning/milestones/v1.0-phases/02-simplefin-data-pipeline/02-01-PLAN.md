---
phase: 02-simplefin-data-pipeline
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - packages/server/src/sync/simplefin-types.ts
  - packages/server/src/sync/simplefin-client.ts
  - packages/server/src/sync/simplefin-client.test.ts
  - packages/server/src/sync/fixtures/simplefin-response.json
autonomous: true
requirements:
  - SYNC-01
must_haves:
  truths:
    - "SimpleFIN client returns normalized accounts with balances as integer cents"
    - "SimpleFIN client returns normalized transactions with amounts as integer cents"
    - "Mock fixture mode returns realistic data without any HTTP calls"
    - "claimToken exchanges a setup token for an access URL"
  artifacts:
    - path: "packages/server/src/sync/simplefin-types.ts"
      provides: "SimpleFIN API response types and app domain types"
      exports: ["SimpleFINAccountSet", "SimpleFINAccount", "SimpleFINTransaction", "NormalizedAccount", "NormalizedTransaction"]
    - path: "packages/server/src/sync/simplefin-client.ts"
      provides: "SimpleFIN HTTP client with mock support"
      exports: ["SimpleFINClient", "createSimpleFINClient", "createMockSimpleFINClient"]
    - path: "packages/server/src/sync/fixtures/simplefin-response.json"
      provides: "Realistic mock SimpleFIN API response"
  key_links:
    - from: "packages/server/src/sync/simplefin-client.ts"
      to: "packages/shared/src/types.ts"
      via: "toCents() for amount normalization"
      pattern: "toCents"
    - from: "packages/server/src/sync/simplefin-client.ts"
      to: "packages/server/src/sync/simplefin-types.ts"
      via: "type imports"
      pattern: "import.*simplefin-types"
---

<objective>
Build the SimpleFIN HTTP client with typed response normalization and mock fixture mode.

Purpose: This is the data source for the entire sync pipeline. All downstream sync logic depends on a reliable, typed client that normalizes SimpleFIN's numeric string amounts to integer cents.
Output: SimpleFIN client module with real and mock implementations, fixture data, and full test coverage.
</objective>

<execution_context>
@/Users/seanspade/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seanspade/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-simplefin-data-pipeline/02-RESEARCH.md
@packages/shared/src/types.ts
@packages/server/src/db/connection.ts

<interfaces>
<!-- Existing types from shared package -->
From packages/shared/src/types.ts:
```typescript
export type Cents = number & { readonly __brand: 'Cents' };
export function toCents(dollars: number): Cents;
```

<!-- SimpleFIN Protocol v2 response shape (from protocol spec) -->
SimpleFIN GET /accounts response:
```json
{
  "errlist": [{"code": "string", "msg": "string", "account_id": "string"}],
  "connections": [{"conn_id": "string", "name": "string", "org_id": "string"}],
  "accounts": [{
    "id": "string",
    "name": "string",
    "conn_id": "string",
    "currency": "string",
    "balance": "numeric string",
    "available-balance": "numeric string (optional)",
    "balance-date": "unix timestamp",
    "transactions": [{
      "id": "string",
      "posted": "unix timestamp (0 if pending)",
      "amount": "numeric string",
      "description": "string",
      "transacted_at": "unix timestamp (optional)",
      "pending": "boolean (optional)"
    }]
  }]
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define SimpleFIN types and create fixture data</name>
  <files>packages/server/src/sync/simplefin-types.ts, packages/server/src/sync/fixtures/simplefin-response.json</files>
  <behavior>
    - SimpleFINAccountSet type matches protocol v2 response shape (errlist, connections, accounts)
    - SimpleFINAccount has id, name, conn_id, currency, balance (string), balance-date (number), transactions
    - SimpleFINTransaction has id, posted (number), amount (string), description (string), pending (boolean optional)
    - NormalizedAccount has id, name, institution, type, balance (Cents), currency, lastSynced
    - NormalizedTransaction has id, accountId, date (ISO string), amount (Cents), pending (boolean), payee, memo, dedupHash
    - Fixture JSON contains 3 accounts: Discover (checking + HELOC), Fidelity (investment), Consumers CU (checking)
    - Fixture includes mix of posted and pending transactions, various amounts (positive deposits, negative withdrawals)
  </behavior>
  <action>
    Create `simplefin-types.ts` with:
    - Raw SimpleFIN API types: `SimpleFINError`, `SimpleFINConnection`, `SimpleFINTransaction`, `SimpleFINAccount`, `SimpleFINAccountSet`
    - App domain types: `NormalizedAccount`, `NormalizedTransaction`
    - The `SimpleFINClient` interface with methods: `fetchAccounts()`, `fetchTransactions(startDate?, endDate?)`, `fetchBalances()`
    - A `claimToken(setupToken: string)` standalone function type

    Create `fixtures/simplefin-response.json` with realistic data:
    - 3 accounts matching user's institutions (Discover, Fidelity, Consumers CU)
    - At least 10 transactions total across accounts with varied payees, dates, amounts
    - Include pending transactions (posted=0, pending=true)
    - Amounts as numeric strings per SimpleFIN spec (e.g., "-45.67", "1234.56")
    - Use realistic payee names (Amazon, Costco, Payroll Deposit, etc.)
  </action>
  <verify>
    <automated>npx vitest run packages/server/src/sync/simplefin-client.test.ts</automated>
  </verify>
  <done>Type definitions compile, fixture JSON is valid and contains 3 accounts with transactions</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement SimpleFIN client with normalization and mock mode</name>
  <files>packages/server/src/sync/simplefin-client.ts, packages/server/src/sync/simplefin-client.test.ts</files>
  <behavior>
    - createMockSimpleFINClient() returns fixture data from JSON file
    - fetchAccounts() returns full SimpleFINAccountSet with accounts and transactions
    - fetchTransactions(startDate, endDate) filters by date range
    - fetchBalances() returns accounts without transactions (balances-only)
    - normalizeAccount(raw) converts SimpleFIN account to NormalizedAccount with balance as Cents
    - normalizeTransaction(raw, accountId) converts SimpleFIN transaction to NormalizedTransaction with amount as Cents
    - Dedup hash is SHA-256 of "accountId|date|amount|payee" truncated to hex
    - Amount "-45.67" normalizes to toCents(-45.67) = -4567
    - Pending transactions have date derived from transacted_at or current date (not posted=0)
    - claimToken(setupToken) decodes Base64 URL and POSTs to /claim endpoint
    - createSimpleFINClient(accessUrl) uses native fetch with credentials from URL
    - Error responses (403, 402) throw descriptive errors
  </behavior>
  <action>
    Write tests first in `simplefin-client.test.ts`:
    - Test normalizeAccount converts balance string to Cents
    - Test normalizeTransaction converts amount string to Cents, generates dedup hash
    - Test mock client fetchAccounts returns all 3 fixture accounts
    - Test mock client fetchBalances returns accounts without transactions
    - Test dedup hash is deterministic (same inputs = same hash)
    - Test pending transaction date handling

    Implement `simplefin-client.ts`:
    - `normalizeAccount(raw: SimpleFINAccount): NormalizedAccount` — parse balance with `toCents(parseFloat(raw.balance))`, derive institution from conn_id/connection name, derive type from account characteristics
    - `normalizeTransaction(raw: SimpleFINTransaction, accountId: string): NormalizedTransaction` — parse amount, convert posted timestamp to ISO date, generate dedup hash, handle pending
    - `generateDedupHash(accountId, date, amount, payee)` — SHA-256 using node:crypto
    - `createMockSimpleFINClient(): SimpleFINClient` — reads fixture JSON, returns it
    - `createSimpleFINClient(accessUrl: string): SimpleFINClient` — uses native fetch, URL already has Basic Auth credentials embedded
    - `claimToken(setupToken: string): Promise<string>` — Base64 decode token to get claim URL, POST to it, return access URL
    - Export factory: `getSimpleFINClient()` checks `SIMPLEFIN_MOCK` env var
  </action>
  <verify>
    <automated>npx vitest run packages/server/src/sync/simplefin-client.test.ts</automated>
  </verify>
  <done>All normalization tests pass, mock client returns typed data, dedup hash is deterministic</done>
</task>

</tasks>

<verification>
- `npx vitest run packages/server/src/sync/` passes all tests
- `simplefin-types.ts` exports all required types
- `simplefin-client.ts` exports `SimpleFINClient`, `createSimpleFINClient`, `createMockSimpleFINClient`, `getSimpleFINClient`, `claimToken`, `normalizeAccount`, `normalizeTransaction`, `generateDedupHash`
- Mock client returns fixture data typed correctly
- Amount normalization: "-45.67" becomes -4567 Cents
</verification>

<success_criteria>
- SimpleFIN client interface defined with fetchAccounts, fetchTransactions, fetchBalances methods
- Mock implementation returns realistic fixture data for 3 accounts
- All amounts normalized from numeric strings to integer cents via toCents()
- Dedup hash generation is deterministic and uses SHA-256
- No live API calls in any test
</success_criteria>

<output>
After completion, create `.planning/phases/02-simplefin-data-pipeline/02-01-SUMMARY.md`
</output>
