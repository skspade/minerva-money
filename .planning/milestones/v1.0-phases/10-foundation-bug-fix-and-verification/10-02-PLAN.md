---
phase: 10-foundation-bug-fix-and-verification
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-foundation/VERIFICATION.md
autonomous: true
requirements:
  - INFR-01
  - INFR-02
  - INFR-03
  - INFR-04
must_haves:
  truths:
    - "VERIFICATION.md exists for Phase 1 at .planning/phases/01-foundation/VERIFICATION.md"
    - "All 4 INFR requirements have pass/fail status with concrete evidence"
    - "INFR-01 references run-backup.ts existence and plist correctness"
    - "INFR-02 references sync-service.ts calling createBackup after sync"
    - "INFR-03 references pruneOldBackups with 30-day retention and minerva_latest.db"
    - "INFR-04 references INTEGER columns for all money values in schema"
  artifacts:
    - path: ".planning/phases/01-foundation/VERIFICATION.md"
      provides: "Formal verification of all Phase 1 INFR requirements"
  key_links:
    - from: ".planning/phases/01-foundation/VERIFICATION.md"
      to: "packages/server/src/backup/run-backup.ts"
      via: "INFR-01 evidence reference"
      pattern: "run-backup"
    - from: ".planning/phases/01-foundation/VERIFICATION.md"
      to: "packages/server/src/sync/sync-service.ts"
      via: "INFR-02 evidence reference"
      pattern: "createBackup"
---

<objective>
Create Phase 1 VERIFICATION.md documenting pass/fail status and concrete evidence for all 4 INFR requirements.

Purpose: The audit report found that INFR-01 through INFR-04 were implemented but never formally verified. This plan creates the verification document that confirms each requirement is satisfied, with specific code references and test results as evidence.

Output: `.planning/phases/01-foundation/VERIFICATION.md` with all 4 requirements verified.
</objective>

<execution_context>
@/Users/seanspade/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seanspade/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@packages/server/src/backup/backup.ts
@packages/server/src/backup/backup.test.ts
@packages/server/src/sync/sync-service.ts
@packages/server/migrations/001-initial-schema.sql
@com.minerva.backup.plist
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gather verification evidence from codebase</name>
  <files>.planning/phases/01-foundation/VERIFICATION.md</files>
  <action>
Inspect the codebase to gather concrete evidence for each requirement, then write VERIFICATION.md.

**INFR-01: Atomic SQLite backups to iCloud Drive every 6 hours via launchd**
Evidence to gather:
- Confirm `packages/server/src/backup/run-backup.ts` exists (created by Plan 10-01 or verify it will exist)
- Confirm `com.minerva.backup.plist` references the correct path
- Confirm plist StartInterval is 21600 (6 hours)
- Confirm `createBackup` in backup.ts uses `db.backup()` (atomic SQLite backup API)
- Confirm DEFAULT_BACKUP_DIR points to iCloud Drive path
- Reference existing test results from backup.test.ts (7 tests)

**INFR-02: Backup after every SimpleFIN sync**
Evidence to gather:
- Confirm `sync-service.ts` imports and calls `createBackup(db)` after sync
- Show the code path: line ~76 calls createBackup inside the sync flow
- Note the try/catch that prevents backup failure from failing the sync

**INFR-03: 30-day retention plus latest copy**
Evidence to gather:
- Confirm `pruneOldBackups` uses RETENTION_DAYS = 30
- Confirm `minerva_latest.db` is written by `createBackup`
- Reference the pruning test in backup.test.ts

**INFR-04: All money values stored as integers (cents)**
Evidence to gather:
- List all money columns from 001-initial-schema.sql with their INTEGER types
- Confirm the schema comment: "All money values are INTEGER (cents)"

Write the verification document at `.planning/phases/01-foundation/VERIFICATION.md` with this structure:

```markdown
# Phase 1: Foundation - Verification

**Verified:** [date]
**Phase Goal:** The project is buildable, the database schema is correct by design, and the backup system is in place before any data is written
**Result:** PASS

## Requirements

### INFR-01: Atomic SQLite backups to iCloud Drive every 6 hours via launchd
**Status:** PASS
**Evidence:**
[bullet points with specific file paths, line numbers, and code references]

### INFR-02: ...
[same structure]

### INFR-03: ...
[same structure]

### INFR-04: ...
[same structure]

## Test Evidence
[Reference backup.test.ts results -- 7 passing tests]

## Summary
[1-2 sentences confirming all requirements satisfied]
```

Note: For INFR-01, reference `run-backup.ts` as existing. Plan 10-01 creates it in the same wave. If verifying before 10-01 completes, note "Created by Plan 10-01" for that specific evidence point.
  </action>
  <verify>
    <automated>test -f .planning/phases/01-foundation/VERIFICATION.md && grep -c "INFR-0" .planning/phases/01-foundation/VERIFICATION.md</automated>
  </verify>
  <done>
    - VERIFICATION.md exists at .planning/phases/01-foundation/VERIFICATION.md
    - All 4 INFR requirements have PASS status with concrete code evidence
    - Evidence includes specific file paths and code references
    - Document confirms Phase 1 goal is achieved
  </done>
</task>

</tasks>

<verification>
1. `cat .planning/phases/01-foundation/VERIFICATION.md` shows the complete verification document
2. All 4 INFR requirements are listed with PASS status
3. Each requirement has specific evidence (file paths, code references, test results)
4. The document references actual source code locations, not vague descriptions
</verification>

<success_criteria>
- VERIFICATION.md exists at .planning/phases/01-foundation/VERIFICATION.md
- INFR-01 verified with run-backup.ts + plist + backup.ts evidence
- INFR-02 verified with sync-service.ts createBackup call evidence
- INFR-03 verified with pruneOldBackups + minerva_latest.db evidence
- INFR-04 verified with INTEGER column listing from schema
- All statuses are PASS with concrete evidence
</success_criteria>

<output>
After completion, create `.planning/phases/10-foundation-bug-fix-and-verification/10-02-SUMMARY.md`
</output>
