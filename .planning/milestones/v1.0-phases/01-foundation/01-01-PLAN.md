---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.base.json
  - vitest.config.ts
  - eslint.config.js
  - packages/client/package.json
  - packages/client/tsconfig.json
  - packages/client/vite.config.ts
  - packages/client/index.html
  - packages/client/src/main.ts
  - packages/server/package.json
  - packages/server/tsconfig.json
  - packages/server/src/index.ts
  - packages/shared/package.json
  - packages/shared/tsconfig.json
  - packages/shared/src/types.ts
autonomous: true
requirements:
  - INFR-04

must_haves:
  truths:
    - "Running `npm run dev` starts both Express server and Vite dev server without errors"
    - "TypeScript compiles with strict mode across all three packages"
    - "Packages can import from @minerva/shared"
    - "Running `npm test` discovers and runs tests from all packages"
  artifacts:
    - path: "package.json"
      provides: "Root workspace config with dev/build/test scripts"
    - path: "packages/server/src/index.ts"
      provides: "Express server entry point listening on port 3001"
    - path: "packages/client/vite.config.ts"
      provides: "Vite config with proxy to Express backend"
    - path: "packages/shared/src/types.ts"
      provides: "Shared TypeScript types (MoneyAmount as number branded type)"
  key_links:
    - from: "packages/client/vite.config.ts"
      to: "packages/server/src/index.ts"
      via: "proxy config pointing /trpc to localhost:3001"
    - from: "package.json"
      to: "packages/*/package.json"
      via: "workspaces field"
---

<objective>
Scaffold the monorepo with npm workspaces: client (Vite + React placeholder), server (Express), and shared (TypeScript types). Configure TypeScript strict mode, ESLint flat config, Vitest with projects discovery, and a `dev` script that runs both servers concurrently.

Purpose: Establish the buildable project foundation so all subsequent phases have a working dev environment.
Output: A running monorepo where `npm run dev` starts both servers and `npm test` runs Vitest.
</objective>

<execution_context>
@/Users/seanspade/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seanspade/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@ARCHITECTURE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create monorepo scaffold with npm workspaces</name>
  <files>
    package.json,
    tsconfig.base.json,
    packages/client/package.json,
    packages/client/tsconfig.json,
    packages/server/package.json,
    packages/server/tsconfig.json,
    packages/shared/package.json,
    packages/shared/tsconfig.json,
    packages/shared/src/types.ts
  </files>
  <action>
    1. Create root `package.json` with:
       - `"name": "minerva-money"`, `"private": true`
       - `"workspaces": ["packages/*"]`
       - Scripts: `"dev"`, `"build"`, `"test"`, `"lint"`
       - `"dev": "concurrently \"npm run dev --workspace=packages/server\" \"npm run dev --workspace=packages/client\""`
       - `"test": "vitest run"`
       - `"lint": "eslint packages/*/src"`

    2. Create `tsconfig.base.json` with shared strict TypeScript config:
       - `"strict": true`, `"target": "ES2022"`, `"module": "Node16"`, `"moduleResolution": "Node16"`
       - `"declaration": true`, `"composite": true`
       - `"skipLibCheck": true`, `"esModuleInterop": true`

    3. Create `packages/shared/package.json`:
       - `"name": "@minerva/shared"`, `"main": "./src/types.ts"`, `"types": "./src/types.ts"`

    4. Create `packages/shared/tsconfig.json` extending base.

    5. Create `packages/shared/src/types.ts` with a `Cents` branded type alias for integer money:
       ```typescript
       export type Cents = number & { readonly __brand: 'Cents' };
       export function toCents(dollars: number): Cents {
         return Math.round(dollars * 100) as Cents;
       }
       ```

    6. Create `packages/server/package.json`:
       - `"name": "@minerva/server"`, `"type": "module"`
       - Dependencies: `better-sqlite3`, `express`
       - DevDependencies: `@types/better-sqlite3`, `@types/express`, `tsx`
       - Scripts: `"dev": "tsx watch src/index.ts"`, `"build": "tsc"`

    7. Create `packages/server/tsconfig.json` extending base, with references to shared.

    8. Create `packages/client/package.json`:
       - `"name": "@minerva/client"`, `"type": "module"`
       - DevDependencies: `vite`
       - Scripts: `"dev": "vite"`, `"build": "vite build"`

    9. Create `packages/client/tsconfig.json` extending base.

    10. Run `npm install` from root to link workspaces and install all dependencies.
        Also install root devDependencies: `concurrently`, `typescript`, `vitest`, `eslint`, `@typescript-eslint/parser`, `@typescript-eslint/eslint-plugin`.
  </action>
  <verify>
    <automated>cd /Users/seanspade/Documents/Source/minverva-money && npm ls --workspaces 2>&1 | head -20</automated>
  </verify>
  <done>npm workspaces resolve all three packages, `npm ls` shows no errors, shared types are importable from server</done>
</task>

<task type="auto">
  <name>Task 2: Configure Vite, Express, ESLint, Vitest, and dev script</name>
  <files>
    packages/client/vite.config.ts,
    packages/client/index.html,
    packages/client/src/main.ts,
    packages/server/src/index.ts,
    vitest.config.ts,
    eslint.config.js
  </files>
  <action>
    1. Create `packages/client/vite.config.ts`:
       - Proxy `/trpc` to `http://localhost:3001`
       - Allow serving files from parent directory (`server.fs.allow: ['..']`) for monorepo

    2. Create `packages/client/index.html` — minimal HTML entry with `<div id="root">` and script tag to `src/main.ts`.

    3. Create `packages/client/src/main.ts` — minimal: `document.getElementById('root')!.textContent = 'Minerva Money';`

    4. Create `packages/server/src/index.ts`:
       - Express app listening on port 3001
       - Health check GET `/health` returning `{ status: 'ok' }`
       - Console log on startup: `Server running on port 3001`

    5. Create root `vitest.config.ts`:
       - `test.projects: ['packages/*']` for monorepo test discovery

    6. Create root `eslint.config.js` (ESLint v9 flat config):
       - TypeScript parser for `packages/*/src/**/*.ts`
       - Recommended rules from `@typescript-eslint`

    7. Create a smoke test `packages/server/src/index.test.ts`:
       - Import and verify Express app creates without errors
       - This validates the Vitest + monorepo config works
  </action>
  <verify>
    <automated>cd /Users/seanspade/Documents/Source/minverva-money && npm test 2>&1 | tail -10</automated>
  </verify>
  <done>`npm run dev` starts both servers (Vite on 5173, Express on 3001), `npm test` passes the smoke test, ESLint runs without config errors</done>
</task>

</tasks>

<verification>
1. `npm run dev` starts both Vite and Express concurrently
2. `curl http://localhost:3001/health` returns `{"status":"ok"}`
3. `http://localhost:5173` shows "Minerva Money"
4. `npm test` runs Vitest and passes
5. `npm run lint` runs ESLint without errors
6. `import { Cents } from '@minerva/shared'` resolves in server code
</verification>

<success_criteria>
- Monorepo scaffold with 3 packages linked via npm workspaces
- TypeScript strict mode compiles all packages
- Dev script runs both servers concurrently
- Vitest discovers tests across packages
- ESLint configured with flat config
- Shared types importable cross-package
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
