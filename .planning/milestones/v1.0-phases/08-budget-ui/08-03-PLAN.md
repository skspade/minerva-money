---
phase: 08-budget-ui
plan: 03
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - packages/client/src/pages/BudgetPage.tsx
autonomous: true
requirements: [BUDG-02]

must_haves:
  truths:
    - "A top-level 'Available to Budget' figure shows unallocated income for the selected month"
    - "Available to Budget displays in green when positive (money to assign)"
    - "Available to Budget displays in red when negative (over-allocated)"
    - "Available to Budget displays in neutral gray when zero"
    - "Available to Budget updates after allocation edits when the query refetches"
  artifacts:
    - path: "packages/client/src/pages/BudgetPage.tsx"
      provides: "Available to Budget header above budget grid"
  key_links:
    - from: "packages/client/src/pages/BudgetPage.tsx"
      to: "budget.summary"
      via: "availableToBudget field from summary query response"
      pattern: "availableToBudget"
---

<objective>
Add a prominent "Available to Budget" header above the budget grid that shows unallocated income for the selected month, with color-coded display.

Purpose: The available-to-budget figure is the single most important number on the budget page. It tells the user how much income they still need to assign to categories. Displaying it prominently with color coding gives instant feedback.
Output: Updated BudgetPage.tsx with Available to Budget header.
</objective>

<execution_context>
@/Users/seanspade/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seanspade/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-budget-ui/08-01-SUMMARY.md

<interfaces>
<!-- budget.summary query response -->
```typescript
// The summary query already returns availableToBudget alongside categories:
// { categories: BudgetCategorySummary[], availableToBudget: number }
// availableToBudget is integer cents: income - totalAllocated - priorOverspending
```

<!-- formatCurrency helper -->
```typescript
export function formatCurrency(cents: number): string;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Available to Budget header above the budget grid</name>
  <files>packages/client/src/pages/BudgetPage.tsx</files>
  <action>
Add the "Available to Budget" section to BudgetPage.tsx, positioned between the month navigation and the budget grid.

**Layout:**
- Place above the budget grid, below the month navigation controls
- Display label "Available to Budget" in `text-sm text-gray-500 uppercase tracking-wide`
- Display the amount prominently using `text-3xl font-bold` with `formatCurrency(data.availableToBudget)`
- The `availableToBudget` value comes from the existing `budget.summary` query response (already fetched in Plan 01). No additional query needed.

**Color coding (per CONTEXT.md locked decisions):**
- Positive (money to assign): `text-green-600`
- Negative (over-allocated): `text-red-600`
- Zero: `text-gray-500`

**Reactivity:**
- Since the available-to-budget value comes from the same `budget.summary` query that the grid uses, it automatically updates whenever that query refetches (including after allocation edits from Plan 02 trigger cache invalidation).
- No special optimistic logic needed; the query refetch handles it.

**Wrapper styling:**
- Container: `mb-6 p-4 bg-white rounded-lg border border-gray-200 text-center` (or left-aligned, use judgment)
- Keep it visually distinct from the grid below
  </action>
  <verify>
    <automated>cd packages/client && npx tsc --noEmit</automated>
  </verify>
  <done>Available to Budget header displays prominently above the grid with the correct color for positive/negative/zero values, and updates when the summary query refetches.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes in client package
- Available to Budget figure visible above the budget grid
- Green when positive, red when negative, gray when zero
- Value updates when navigating to different months
- Value updates after allocation changes (when query refetches)
</verification>

<success_criteria>
- Available to Budget header is prominently displayed using data from the summary query
- Color coding correctly reflects positive/negative/zero states
- No additional API calls needed beyond the existing summary query
</success_criteria>

<output>
After completion, create `.planning/phases/08-budget-ui/08-03-SUMMARY.md`
</output>
