---
phase: 13-transaction-filter-completion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/client/src/pages/TransactionsPage.tsx
  - packages/client/src/pages/TransactionsPage.test.tsx
autonomous: true
requirements:
  - ACCT-04
must_haves:
  truths:
    - "User can filter transactions by minimum amount"
    - "User can filter transactions by maximum amount"
    - "User can filter transactions by category from a dropdown"
    - "User can filter to show only uncategorized transactions"
    - "Amount range, category, date range, and payee/memo filters all work together"
  artifacts:
    - path: "packages/client/src/pages/TransactionsPage.tsx"
      provides: "Amount range inputs, category dropdown, extended filter logic"
      contains: "amountMin"
  key_links:
    - from: "packages/client/src/pages/TransactionsPage.tsx"
      to: "useMemo filtered array"
      via: "amountMin/amountMax/categoryFilter state in useMemo deps"
      pattern: "Math\\.abs.*amount"
---

<objective>
Add amount range and category dropdown filters to TransactionsPage to complete ACCT-04.

Purpose: Users need to filter transactions by all four criteria (date, payee, amount, category). Date and payee filters already work. This plan adds amount range (min/max) and category dropdown filters.
Output: TransactionsPage.tsx with all four filter types working together.
</objective>

<execution_context>
@/Users/seanspade/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seanspade/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/13-transaction-filter-completion/13-CONTEXT.md
@.planning/phases/13-transaction-filter-completion/13-RESEARCH.md
@packages/client/src/pages/TransactionsPage.tsx
@packages/client/src/components/CategoryPicker.tsx

<interfaces>
From packages/client/src/pages/TransactionsPage.tsx:
- Existing state: inputValue, debouncedSearch, sortColumn, sortDirection, dateFrom, dateTo
- Existing filter: useMemo `filtered` at line 96 chains dateFrom, dateTo, debouncedSearch filters then sorts
- Existing filter bar: flex flex-wrap items-end gap-4 mb-4 container at line 170
- categoryGroups already fetched at line 16 via trpc.categories.groups.list.queryOptions()
- Transaction shape includes: amount (integer cents), categoryId (number | null), payee, memo, date, accountName

From packages/client/src/components/CategoryPicker.tsx:
- Renders <select> with <optgroup> for each category group
- groups?.map(group => <optgroup label={group.name}> with group.categories.map(cat => <option value={cat.id}>)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add filter state, logic, and UI for amount range and category</name>
  <files>packages/client/src/pages/TransactionsPage.tsx, packages/client/src/pages/TransactionsPage.test.tsx</files>
  <behavior>
    - Amount min filter: entering "50" filters to transactions with abs(amount) >= 5000 cents
    - Amount max filter: entering "100" filters to transactions with abs(amount) <= 10000 cents
    - Both min and max together: shows transactions in the range
    - Category filter "uncategorized": shows only transactions with categoryId === null
    - Category filter specific ID: shows only transactions with matching categoryId
    - Category filter empty string (All): shows all transactions (no category filtering)
    - All filters combine: date + search + amount + category all apply together
  </behavior>
  <action>
    1. Add three new useState hooks after existing state declarations:
       - `const [amountMin, setAmountMin] = useState('');`
       - `const [amountMax, setAmountMax] = useState('');`
       - `const [categoryFilter, setCategoryFilter] = useState('');`

    2. Extend the `filtered` useMemo block (after the debouncedSearch filter, before the sort) with:
       - Amount min filter: if amountMin is non-empty, parse with `Math.round(parseFloat(amountMin) * 100)` and filter where `Math.abs(t.amount) >= minCents`
       - Amount max filter: if amountMax is non-empty, parse with `Math.round(parseFloat(amountMax) * 100)` and filter where `Math.abs(t.amount) <= maxCents`
       - Category filter: if categoryFilter === 'uncategorized', filter where `t.categoryId === null`. If categoryFilter is non-empty (and not 'uncategorized'), parse as int and filter where `t.categoryId === catId`
       - Add amountMin, amountMax, categoryFilter to the useMemo dependency array

    3. Add amount range inputs to the filter bar (after the date-to div, before the closing </div> of the filter bar):
       - A div with flex items-center gap-2 containing:
         - Label "Min $" (text-sm text-gray-600)
         - input type="number" step="0.01" placeholder="0.00" value={amountMin} onChange -> setAmountMin, class matching date inputs: "w-24 px-2 py-2 border border-gray-300 rounded-md text-sm"
       - Another div with flex items-center gap-2 containing:
         - Label "Max $" (text-sm text-gray-600)
         - input type="number" step="0.01" placeholder="0.00" value={amountMax} onChange -> setAmountMax, same styling

    4. Add category dropdown to the filter bar (after amount inputs):
       - A div with flex items-center gap-2 containing:
         - Label "Category" (text-sm text-gray-600)
         - select element with value={categoryFilter} onChange -> setCategoryFilter
         - Class: "px-2 py-2 border border-gray-300 rounded-md text-sm"
         - Options: <option value="">All Categories</option>, <option value="uncategorized">Uncategorized</option>
         - Then categoryGroups?.map(group => <optgroup key={group.id} label={group.name}> with group.categories.map(cat => <option key={cat.id} value={cat.id}>{cat.name}</option>))
  </action>
  <verify>
    <automated>cd /Users/seanspade/Documents/Source/minverva-money && npx vitest run packages/client/src/pages/TransactionsPage.test.tsx</automated>
  </verify>
  <done>
    - TransactionsPage renders min/max amount inputs and category dropdown in filter bar
    - Amount range filter narrows transaction list by absolute amount value
    - Category dropdown filters by specific category or uncategorized
    - All four filter types (date, search, amount, category) combine correctly
    - TypeScript compiles without errors
  </done>
</task>

</tasks>

<verification>
- `npx vitest run` passes all tests
- `npx tsc --noEmit` compiles without errors
- TransactionsPage renders amount min, amount max, and category filter controls
- Filtering by amount range works with dollar values converted to cents
- Filtering by category shows matching transactions only
- "Uncategorized" option shows transactions with null categoryId
- All filters combine (date + search + amount + category)
</verification>

<success_criteria>
- ACCT-04 is fully satisfied: transaction list supports date, payee, amount, and category filtering
- Amount filter uses absolute value comparison (both debits and credits filterable by magnitude)
- Category dropdown follows existing CategoryPicker optgroup pattern
- Filter bar layout is consistent with existing date/search controls
</success_criteria>

<output>
After completion, create `.planning/phases/13-transaction-filter-completion/13-01-SUMMARY.md`
</output>
