---
phase: 05-categorization-rules-engine
plan: 04
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  - packages/client/src/pages/TransactionsPage.tsx
autonomous: true
requirements:
  - CATG-05

must_haves:
  truths:
    - "Transaction rows show which rule categorized them"
    - "Rule match explanation shows why the rule matched (e.g., 'Matched merchant contains Amazon')"
    - "User can manually override a rule-assigned category and the rule attribution is cleared"
  artifacts:
    - path: "packages/client/src/pages/TransactionsPage.tsx"
      provides: "Rule attribution display and manual override"
  key_links:
    - from: "packages/client/src/pages/TransactionsPage.tsx"
      to: "trpc.transactions.list"
      via: "ruleName field in response"
      pattern: "ruleName"
---

<objective>
Add rule attribution display to the transactions page. Show which rule categorized each transaction and allow manual override that clears the rule association.

Purpose: Users need to see which rule won and why to trust the automation. Manual override ensures user control.
Output: Transaction rows display rule name and match reason, with manual override capability.
</objective>

<execution_context>
@/Users/seanspade/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seanspade/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-categorization-rules-engine/05-02-SUMMARY.md
@packages/client/src/pages/TransactionsPage.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rule attribution display on transaction rows</name>
  <files>packages/client/src/pages/TransactionsPage.tsx</files>
  <action>
    Update TransactionsPage to show rule attribution in the Category column:

    1. The transactions.list response now includes ruleId and ruleName (from 05-02). Update the type usage accordingly.

    2. For each transaction row in the Category column:
       - If transaction has a ruleName (rule_id is not null): show the category name with a small label below it showing the rule name, e.g., category name in normal text, then below in smaller gray text "Rule: {ruleName}". Use a tooltip or title attribute with the match explanation.
       - The CategoryPicker should still be shown so users can change the category manually.

    3. When a user changes the category via CategoryPicker on a rule-categorized transaction:
       - The existing updateCategory mutation already works (it sets category_id and the server clears rule_id since updateTransactionCategory doesn't set rule_id)
       - Update the optimistic update to also clear ruleName when manually changing category
       - This effectively creates a manual override: the transaction now has category_id set but rule_id NULL

    4. Keep the existing optimistic update pattern but add ruleId: null and ruleName: null to the optimistic response when a user manually changes category.
  </action>
  <verify>
    <automated>npx vitest run</automated>
  </verify>
  <done>Rule-categorized transactions show rule name below category, manual category change clears rule attribution in UI</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Server-side rule clearing on manual override</name>
  <files>packages/server/src/categories/category-service.ts, packages/server/src/categories/category-service.test.ts</files>
  <behavior>
    - When updateTransactionCategory is called, it should also set rule_id = NULL on the transaction
    - This ensures manual categorization overrides rule assignments
    - Test: create a transaction with rule_id set, call updateTransactionCategory, verify rule_id is now NULL
  </behavior>
  <action>
    Update updateTransactionCategory in category-service.ts:
    - Change the UPDATE statement to also set rule_id = NULL: `UPDATE transactions SET category_id = ?, rule_id = NULL WHERE id = ?`
    - This ensures that when a user manually assigns a category, the rule association is cleared

    Add a test to category-service.test.ts:
    - Insert a transaction with a rule_id value
    - Call updateTransactionCategory to change its category
    - Verify the transaction's rule_id is now NULL
  </action>
  <verify>
    <automated>npx vitest run packages/server/src/categories/category-service.test.ts</automated>
  </verify>
  <done>updateTransactionCategory clears rule_id, preventing rules from re-overriding manual categorization</done>
</task>

</tasks>

<verification>
- Transaction rows show rule name when categorized by a rule
- Manually changing category clears rule_id on the server
- Optimistic update clears ruleName in UI immediately
- `npx vitest run` passes all tests
</verification>

<success_criteria>
- Rule-categorized transactions display rule name in the category column
- Manual override clears rule_id server-side (via updateTransactionCategory setting rule_id = NULL)
- Optimistic update reflects the override immediately in the UI
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/05-categorization-rules-engine/05-04-SUMMARY.md`
</output>
