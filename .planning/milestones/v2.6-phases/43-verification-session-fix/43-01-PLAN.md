---
phase: 43-verification-session-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/client/src/hooks/useStreamingChat.ts
  - packages/client/src/hooks/useStreamingChat.test.ts
  - .planning/phases/40-express-sse-endpoint/40-VERIFICATION.md
  - .planning/phases/42-chatpage-streaming-ui/42-VERIFICATION.md
  - .planning/phases/39-server-stream-processing/39-01-SUMMARY.md
  - .planning/phases/40-express-sse-endpoint/40-01-SUMMARY.md
  - .planning/REQUIREMENTS.md
autonomous: true
gap_closure: true
requirements: [SRVR-01, SRVR-02, SRVR-03, SRVR-04, SRVR-05, SRVR-06, UI-01, UI-02, UI-03, UI-04, UI-05, UI-06]

must_haves:
  truths:
    - "processStream onDone receives original sessionId when no session SSE event is emitted during resumed turn"
    - "40-VERIFICATION.md confirms SRVR-01 and SRVR-02 pass with evidence"
    - "42-VERIFICATION.md confirms UI-01 through UI-06 pass with evidence"
    - "39-01-SUMMARY.md has requirements-completed in YAML frontmatter"
    - "40-01-SUMMARY.md has YAML frontmatter with requirements-completed"
    - "All 20 v2.6 requirement checkboxes in REQUIREMENTS.md are checked"
    - "All 20 traceability table entries show Done status"
  artifacts:
    - path: ".planning/phases/40-express-sse-endpoint/40-VERIFICATION.md"
      provides: "Phase 40 verification with SRVR-01, SRVR-02 evidence"
    - path: ".planning/phases/42-chatpage-streaming-ui/42-VERIFICATION.md"
      provides: "Phase 42 verification with UI-01 through UI-06 evidence"
  key_links:
    - from: "useStreamingChat.ts processStream case 'done'"
      to: "ChatPage setSessionId"
      via: "handlers.onDone(event.text, capturedSessionId || options.sessionId || '')"
      pattern: "capturedSessionId \\|\\| options\\.sessionId"
---

<objective>
Fix session ID continuity bug in useStreamingChat, create missing verification docs for phases 40 and 42, fix SUMMARY.md frontmatter, and check all v2.6 requirement checkboxes.

Purpose: Close all gaps identified in the v2.6 milestone audit so the milestone can be marked complete.
Output: Bug fix with regression test, two verification docs, corrected SUMMARY frontmatter, fully checked REQUIREMENTS.md.
</objective>

<execution_context>
@/Users/seanspade/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seanspade/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-verification-session-fix/43-CONTEXT.md
@packages/client/src/hooks/useStreamingChat.ts
@packages/client/src/hooks/useStreamingChat.test.ts
@.planning/phases/39-server-stream-processing/39-VERIFICATION.md
@.planning/phases/39-server-stream-processing/39-01-SUMMARY.md
@.planning/phases/40-express-sse-endpoint/40-01-SUMMARY.md
@.planning/phases/42-chatpage-streaming-ui/42-01-SUMMARY.md

<interfaces>
<!-- Key types the executor needs for the session ID fix -->

From packages/client/src/hooks/useStreamingChat.ts:
```typescript
export interface StreamHandlers {
  onTextDelta: (text: string) => void;
  onToolStart: (tool: string) => void;
  onToolEnd: (tool: string) => void;
  onDone: (text: string, sessionId: string) => void;
  onError: (message: string) => void;
  onSession: (sessionId: string) => void;
}

export interface StreamOptions {
  sessionId?: string;
  model?: string;
  signal?: AbortSignal;
}

export async function processStream(
  message: string,
  handlers: StreamHandlers,
  options: StreamOptions = {},
): Promise<void>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix session ID continuity bug with regression test</name>
  <files>packages/client/src/hooks/useStreamingChat.ts, packages/client/src/hooks/useStreamingChat.test.ts</files>
  <behavior>
    - Test: processStream onDone receives options.sessionId when no session SSE event is emitted (simulates resumed turn where Agent SDK skips init message)
    - Setup: mockFetchSuccess with only text-delta and done events (no session event), call processStream with options.sessionId = 'existing-sess-123'
    - Expected: onDone called with ('Full text', 'existing-sess-123') not ('Full text', '')
  </behavior>
  <action>
    1. RED: Add a test to the "useStreamingChat (integration)" describe block:
       ```
       it('falls back to options.sessionId in onDone when no session event is emitted', async () => {
         mockFetchSuccess([
           { type: 'text-delta', text: 'Resumed' },
           { type: 'done', text: 'Resumed response' },
         ]);
         const { processStream } = await import('./useStreamingChat');
         const onDone = vi.fn();
         const handlers = {
           onTextDelta: vi.fn(), onToolStart: vi.fn(), onToolEnd: vi.fn(),
           onDone, onError: vi.fn(), onSession: vi.fn(),
         };
         await processStream('test', handlers, { sessionId: 'existing-sess-123' });
         expect(onDone).toHaveBeenCalledWith('Resumed response', 'existing-sess-123');
       });
       ```
    2. Run test — it should FAIL because current code passes empty capturedSessionId to onDone.
    3. GREEN: In useStreamingChat.ts line 119, change:
       `handlers.onDone(event.text, capturedSessionId);`
       to:
       `handlers.onDone(event.text, capturedSessionId || options.sessionId || '');`
       where `options` is destructured at the top of processStream — use the `sessionId` from the destructured `const { sessionId, model, signal } = options;` but note there's a naming collision with the destructured `sessionId`. Instead, reference `options.sessionId` directly (change the destructure if needed) or use a renamed variable.
       Simplest fix: change the `case 'done':` line to:
       `handlers.onDone(event.text, capturedSessionId || sessionId || '');`
       since `sessionId` is already destructured from options on line 67.
    4. Run test — it should PASS.
    5. Verify existing test "fires onDone with full text and sessionId from session event" still passes (capturedSessionId takes priority when session event IS emitted).
  </action>
  <verify>
    <automated>npx vitest run packages/client/src/hooks/useStreamingChat.test.ts</automated>
  </verify>
  <done>New regression test passes. All existing tests still pass. The onDone handler falls back to options.sessionId when capturedSessionId is empty.</done>
</task>

<task type="auto">
  <name>Task 2: Create verification docs and fix SUMMARY frontmatter</name>
  <files>.planning/phases/40-express-sse-endpoint/40-VERIFICATION.md, .planning/phases/42-chatpage-streaming-ui/42-VERIFICATION.md, .planning/phases/39-server-stream-processing/39-01-SUMMARY.md, .planning/phases/40-express-sse-endpoint/40-01-SUMMARY.md</files>
  <action>
    1. Create 40-VERIFICATION.md following the format of 39-VERIFICATION.md:
       - Phase Goal: Server exposes a validated SSE endpoint for streaming chat
       - SRVR-01: POST /api/chat/stream endpoint — PASS (createChatStreamHandler wires Express route, tests verify request/response)
       - SRVR-02: Zod validation with pre-SSE error response — PASS (Zod schema validates message/sessionId/model, 400 returned before SSE headers on invalid input, tests verify)
       - Must-haves: endpoint registered at POST /api/chat/stream, Zod validation rejects missing/empty message, invalid model returns 400 before SSE headers, SSE headers set correctly, req.close wired to AbortController, events written as data: JSON lines
       - Artifacts: packages/server/src/agent/chat-stream-handler.ts, packages/server/src/agent/chat-stream-handler.test.ts (10 tests)
       - Automated check: `npx vitest run packages/server/src/agent/chat-stream-handler.test.ts`
       - Score: all must-haves verified, both requirements satisfied

    2. Create 42-VERIFICATION.md following the same format:
       - Phase Goal: ChatPage renders streaming responses with incremental text, tool indicators, and smart UX
       - UI-01: Incremental text in bubble — PASS (streamingText rendered via react-markdown in live bubble)
       - UI-02: Tool activity indicator — PASS (getToolLabel() maps 24 tools to labels, conditional indicator rendering)
       - UI-03: Smart auto-scroll — PASS (userScrolledUpRef gates auto-scroll, scroll listener pauses on user scroll-up)
       - UI-04: Bouncing dots — PASS (dots show only when isStreaming && no streamingText && no activeTool)
       - UI-05: Confirmation buttons — PASS (parseConfirmation runs on completed text in onComplete callback)
       - UI-06: Input disabled — PASS (isStreaming state disables input field)
       - Must-haves: live bubble renders streamingText separately from messages array, tool indicator shows human-readable label, auto-scroll pauses on user scroll-up, dots disappear after first text token, confirmation buttons appear after stream completes, input disabled during streaming
       - Artifacts: packages/client/src/pages/ChatPage.tsx, packages/client/src/utils/tool-labels.ts, packages/client/src/utils/tool-labels.test.ts (5 tests)
       - Automated check: `npx vitest run packages/client/src/utils/tool-labels.test.ts`
       - Score: all must-haves verified, all 6 requirements satisfied

    3. Fix 39-01-SUMMARY.md: Add `requirements-completed: [SRVR-03, SRVR-04, SRVR-05, SRVR-06]` to the existing YAML frontmatter block (after the `completed` field).

    4. Fix 40-01-SUMMARY.md: Add a YAML frontmatter block at the very top of the file:
       ```yaml
       ---
       phase: 40-express-sse-endpoint
       plan: 01
       status: complete
       started: 2026-03-25
       completed: 2026-03-25
       requirements-completed: [SRVR-01, SRVR-02]
       ---
       ```
       Keep all existing content below the frontmatter.
  </action>
  <verify>
    <automated>ls .planning/phases/40-express-sse-endpoint/40-VERIFICATION.md .planning/phases/42-chatpage-streaming-ui/42-VERIFICATION.md && grep "requirements-completed" .planning/phases/39-server-stream-processing/39-01-SUMMARY.md .planning/phases/40-express-sse-endpoint/40-01-SUMMARY.md</automated>
  </verify>
  <done>Both verification docs exist with requirement evidence tables. 39-01-SUMMARY.md and 40-01-SUMMARY.md have requirements-completed in YAML frontmatter.</done>
</task>

<task type="auto">
  <name>Task 3: Check all v2.6 requirement checkboxes and update traceability</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
    1. In .planning/REQUIREMENTS.md, change all 20 v2.6 requirement checkboxes from `- [ ]` to `- [x]`:
       - PROTO-01, PROTO-02, PROTO-03
       - SRVR-01 through SRVR-06
       - CLNT-01 through CLNT-05
       - UI-01 through UI-06

    2. In the Traceability table, update all 20 rows:
       - Change `Status` column from `Pending` to `Done` for every requirement
       - Fix the Phase column: SRVR-01 and SRVR-02 should map to Phase 40 (not Phase 43), SRVR-03 through SRVR-06 to Phase 39, UI-01 through UI-06 to Phase 42 (not Phase 43)
       - The traceability table currently shows SRVR-01..06 and UI-01..06 mapped to Phase 43, but these were implemented in phases 39/40/42 — Phase 43 only verifies them. Update the Phase column to the implementing phase.
  </action>
  <verify>
    <automated>grep -c "\- \[x\]" .planning/REQUIREMENTS.md | grep "20" && grep -c "| Done |" .planning/REQUIREMENTS.md | grep "20"</automated>
  </verify>
  <done>All 20 v2.6 requirement checkboxes are checked. All 20 traceability table entries show Done status with correct implementing phase.</done>
</task>

</tasks>

<verification>
1. `npx vitest run packages/client/src/hooks/useStreamingChat.test.ts` — all tests pass including new regression test
2. `ls .planning/phases/40-express-sse-endpoint/40-VERIFICATION.md .planning/phases/42-chatpage-streaming-ui/42-VERIFICATION.md` — both exist
3. `grep "requirements-completed" .planning/phases/39-server-stream-processing/39-01-SUMMARY.md .planning/phases/40-express-sse-endpoint/40-01-SUMMARY.md` — both have the field
4. `grep -c "\- \[x\]" .planning/REQUIREMENTS.md` — returns 20
5. `grep -c "| Done |" .planning/REQUIREMENTS.md` — returns 20
</verification>

<success_criteria>
- Session ID bug fixed: processStream onDone falls back to options.sessionId when capturedSessionId is empty
- Regression test added and passing
- All existing useStreamingChat tests still pass
- 40-VERIFICATION.md exists with SRVR-01, SRVR-02 evidence
- 42-VERIFICATION.md exists with UI-01 through UI-06 evidence
- 39-01-SUMMARY.md has requirements-completed field
- 40-01-SUMMARY.md has full YAML frontmatter with requirements-completed field
- All 20 v2.6 checkboxes in REQUIREMENTS.md are checked
- All 20 traceability entries show Done with correct implementing phase
</success_criteria>

<output>
After completion, create `.planning/phases/43-verification-session-fix/43-01-SUMMARY.md`
</output>
