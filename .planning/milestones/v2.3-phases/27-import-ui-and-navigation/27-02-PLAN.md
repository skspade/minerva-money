---
phase: 27-import-ui-and-navigation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/client/src/app.tsx
  - packages/client/src/components/Layout.tsx
  - packages/client/src/components/MoreSheet.tsx
autonomous: true
requirements:
  - NAV-01
  - NAV-02
  - NAV-03

must_haves:
  truths:
    - "Import page is accessible at /import route"
    - "Import link appears in desktop navigation bar"
    - "Import appears in mobile More bottom sheet"
  artifacts:
    - path: "packages/client/src/app.tsx"
      provides: "Route definition for /import"
      contains: "path=\"import\""
    - path: "packages/client/src/components/Layout.tsx"
      provides: "Desktop nav link for Import"
      contains: "to=\"/import\""
    - path: "packages/client/src/components/MoreSheet.tsx"
      provides: "Mobile nav entry for Import"
      contains: "'/import'"
  key_links:
    - from: "packages/client/src/app.tsx"
      to: "packages/client/src/pages/ImportPage.tsx"
      via: "Route element prop"
      pattern: "ImportPage"
    - from: "packages/client/src/components/Layout.tsx"
      to: "/import route"
      via: "NavLink to='/import'"
      pattern: "to=\"/import\""
    - from: "packages/client/src/components/MoreSheet.tsx"
      to: "/import route"
      via: "MORE_LINKS array entry"
      pattern: "'/import'"
---

<objective>
Wire the Import page into the app's navigation — add the route, desktop nav link, and mobile More sheet entry.

Purpose: Makes the Import page discoverable and accessible from both desktop and mobile navigation.
Output: Updated `app.tsx`, `Layout.tsx`, and `MoreSheet.tsx`
</objective>

<execution_context>
@/Users/seanspade/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seanspade/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@packages/client/src/app.tsx
@packages/client/src/components/Layout.tsx
@packages/client/src/components/MoreSheet.tsx

<interfaces>
<!-- Existing patterns from the codebase -->

From packages/client/src/app.tsx:
```typescript
// Routes inside <Route element={<Layout />}>
// Last route: <Route path="chat" element={<ChatPage />} />
// Pattern: import PageName from './pages/PageNamePage';
```

From packages/client/src/components/Layout.tsx:
```typescript
// Desktop nav uses NavLink components with this pattern:
// <NavLink to="/path" className={({ isActive }) => `px-3 py-1 rounded ${isActive ? 'bg-gray-700' : 'hover:bg-gray-800'}`}>Label</NavLink>
// Last NavLink: Chat (line 79-86)
```

From packages/client/src/components/MoreSheet.tsx:
```typescript
// MORE_LINKS array: { to: string, icon: LucideIcon, label: string }[]
// Existing icons: CreditCard, Tag, Sliders, ArrowLeftRight, BarChart from lucide-react
// Upload icon available from lucide-react
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add import route and navigation entries</name>
  <files>packages/client/src/app.tsx, packages/client/src/components/Layout.tsx, packages/client/src/components/MoreSheet.tsx</files>
  <action>
**1. app.tsx — Add route:**
- Add import at top: `import ImportPage from './pages/ImportPage';`
- Add route inside the Layout route, after the chat route: `<Route path="import" element={<ImportPage />} />`

**2. Layout.tsx — Add desktop NavLink:**
- Add a new NavLink after the Chat NavLink (after line 86), following the exact same pattern:
```tsx
<NavLink
  to="/import"
  className={({ isActive }) =>
    `px-3 py-1 rounded ${isActive ? 'bg-gray-700' : 'hover:bg-gray-800'}`
  }
>
  Import
</NavLink>
```

**3. MoreSheet.tsx — Add mobile entry:**
- Add `Upload` to the lucide-react import: `import { CreditCard, Tag, Sliders, ArrowLeftRight, BarChart, Upload } from 'lucide-react';`
- Add entry to the `MORE_LINKS` array after the Reports entry: `{ to: '/import', icon: Upload, label: 'Import' }`
  </action>
  <verify>
    <automated>cd /Users/seanspade/Documents/Source/minerva-money && npx tsc --noEmit 2>&1 | head -20 && grep -n "import" packages/client/src/app.tsx | grep -i import && grep -n "/import" packages/client/src/components/Layout.tsx && grep -n "import" packages/client/src/components/MoreSheet.tsx | grep -i upload</automated>
  </verify>
  <done>Import route exists at /import in app.tsx, "Import" NavLink appears in desktop nav bar in Layout.tsx after Chat, and Import entry with Upload icon exists in MORE_LINKS array in MoreSheet.tsx</done>
</task>

</tasks>

<verification>
- TypeScript compiles: `npx tsc --noEmit`
- Route exists: `grep 'path="import"' packages/client/src/app.tsx`
- Desktop nav: `grep 'to="/import"' packages/client/src/components/Layout.tsx`
- Mobile nav: `grep "'/import'" packages/client/src/components/MoreSheet.tsx`
- Upload icon imported: `grep 'Upload' packages/client/src/components/MoreSheet.tsx`
</verification>

<success_criteria>
- /import route is defined in app.tsx and renders ImportPage
- Desktop nav bar shows "Import" link after "Chat"
- Mobile "More" bottom sheet includes "Import" with Upload icon
- All three files compile without TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/27-import-ui-and-navigation/27-02-SUMMARY.md`
</output>
